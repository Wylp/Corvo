# Corvo Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir um app macOS accessory que captura o histórico da área de
transferência em background e o expõe numa janela flutuante acionada por atalho
global, com organização automática por app de origem e manual por tags.

**Architecture:** Um poller do `NSPasteboard.changeCount` alimenta um banco
SQLite via GRDB; a UI SwiftUI é hospedada num `NSPanel` flutuante e observa o
banco por `ValueObservation`. As camadas não se conhecem em círculo: `Clipboard/`
escreve no `Storage/`, `UI/` só lê `Storage/`, e `System/` (atalho, login item)
não conhece nenhuma das duas.

**Tech Stack:** Swift 6, SwiftUI, macOS 14+, GRDB.swift 7.11.1, XcodeGen 2.46,
Swift Testing, Carbon HIToolbox (atalho global), ServiceManagement (login item).

## Global Constraints

- Target mínimo **macOS 14.0**. Swift language mode 6.
- **Sem App Sandbox.** `ENABLE_HARDENED_RUNTIME = YES`, assinatura automática local.
- `LSUIElement = true` — sem ícone no Dock.
- Bundle ID **`com.wylp.corvo`**, produto `Corvo.app`.
- **Uma única dependência de runtime:** GRDB.swift. Nenhuma outra pode ser
  adicionada sem alterar o spec. XcodeGen é ferramenta de build, não dependência.
- `Corvo.xcodeproj` é **gerado** por XcodeGen e fica no `.gitignore`. A fonte da
  verdade do projeto é `project.yml`.
- **Desvio deliberado do spec:** as colunas do SQLite usam camelCase
  (`sourceBundleId`) em vez do snake_case escrito no spec. Motivo: GRDB mapeia
  colunas para propriedades pelo nome, e snake_case exigiria um bloco
  `CodingKeys` em todo record. Ganho zero, boilerplate em todo lugar.
- Todo texto visível ao usuário é em **português do Brasil**.
- Toda captura de clipboard passa obrigatoriamente pelo filtro de privacidade da
  Task 6. Nenhum caminho de escrita pode contorná-lo.
- Comentários que marcam simplificação deliberada usam o prefixo `// ponytail:`
  e nomeiam o teto e o caminho de upgrade.

## Estrutura de arquivos

| Arquivo | Responsabilidade |
|---|---|
| `project.yml` | definição do projeto Xcode (XcodeGen) |
| `Corvo-Info.plist` | Info.plist do app |
| `Sources/Corvo/App/CorvoApp.swift` | `@main`, `MenuBarExtra`, ciclo de vida |
| `Sources/Corvo/App/AppEnvironment.swift` | montagem das dependências |
| `Sources/Corvo/Storage/ClipItem.swift` | record do item + `ClipKind` |
| `Sources/Corvo/Storage/Tag.swift` | records `Tag` e `ItemTag` |
| `Sources/Corvo/Storage/AppDatabase.swift` | abertura do banco + migrações |
| `Sources/Corvo/Storage/BlobStore.swift` | imagens em disco + coleta de órfãos |
| `Sources/Corvo/Storage/ItemRepository.swift` | inserção, busca, filtros, tags |
| `Sources/Corvo/Storage/Retention.swift` | poda por idade e quantidade |
| `Sources/Corvo/Clipboard/PasteboardReading.swift` | protocolo do pasteboard |
| `Sources/Corvo/Clipboard/SourceTracker.swift` | heurística do app de origem |
| `Sources/Corvo/Clipboard/PasteboardMonitor.swift` | poller + filtro de privacidade |
| `Sources/Corvo/Clipboard/Paster.swift` | colar de volta via CGEvent |
| `Sources/Corvo/System/GlobalHotkey.swift` | `RegisterEventHotKey` |
| `Sources/Corvo/System/LoginItem.swift` | `SMAppService` |
| `Sources/Corvo/System/Preferences.swift` | `UserDefaults` tipado |
| `Sources/Corvo/UI/FloatingPanel.swift` | `NSPanel` + hospedagem SwiftUI |
| `Sources/Corvo/UI/HistoryView.swift` | busca + carrossel + teclado |
| `Sources/Corvo/UI/ItemCard.swift` | card individual |
| `Sources/Corvo/UI/FilterSidebar.swift` | filtros por fonte e tag |
| `Sources/Corvo/UI/PreferencesView.swift` | janela de preferências |
| `Tests/CorvoTests/*.swift` | testes das quatro áreas do spec |

---

### Task 1: Esqueleto do projeto

Entrega: `Corvo.app` compila, roda como accessory, mostra ícone na menu bar, e
`xcodebuild test` executa um teste trivial.

**Files:**
- Create: `project.yml`
- Create: `Corvo-Info.plist`
- Create: `Sources/Corvo/App/CorvoApp.swift`
- Create: `Tests/CorvoTests/SmokeTests.swift`
- Create: `.gitignore`
- Create: `Makefile`

**Interfaces:**
- Consumes: nada
- Produces: target `Corvo` e target de teste `CorvoTests`; scheme `Corvo`

- [ ] **Step 1: Instalar o XcodeGen**

```bash
brew install xcodegen
xcodegen --version
```

Esperado: `Version: 2.46.0` ou superior.

- [ ] **Step 2: Escrever `.gitignore`**

```gitignore
.DS_Store
Corvo.xcodeproj/
build/
DerivedData/
*.xcuserstate
.swiftpm/
```

- [ ] **Step 3: Escrever `project.yml`**

```yaml
name: Corvo
options:
  bundleIdPrefix: com.wylp
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true

packages:
  GRDB:
    url: https://github.com/groue/GRDB.swift
    from: 7.11.1

targets:
  Corvo:
    type: application
    platform: macOS
    sources:
      - Sources/Corvo
    dependencies:
      - package: GRDB
        product: GRDB
    info:
      path: Corvo-Info.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.wylp.corvo
        PRODUCT_NAME: Corvo
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
        CODE_SIGN_STYLE: Automatic
        ENABLE_HARDENED_RUNTIME: YES
        GENERATE_INFOPLIST_FILE: NO

  CorvoTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests/CorvoTests
    dependencies:
      - target: Corvo
      - package: GRDB
        product: GRDB
    settings:
      base:
        SWIFT_VERSION: "6.0"
        GENERATE_INFOPLIST_FILE: YES

schemes:
  Corvo:
    build:
      targets:
        Corvo: all
        CorvoTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - CorvoTests
```

- [ ] **Step 4: Escrever `Corvo-Info.plist`**

`LSUIElement` é o que remove o ícone do Dock — sem ele o app vira uma janela
normal e todo o desenho da menu bar deixa de fazer sentido.

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>pt_BR</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>LSMinimumSystemVersion</key>
    <string>14.0</string>
    <key>LSUIElement</key>
    <true/>
    <key>NSHumanReadableCopyright</key>
    <string>MIT</string>
</dict>
</plist>
```

- [ ] **Step 5: Escrever `Sources/Corvo/App/CorvoApp.swift`**

```swift
import SwiftUI

@main
struct CorvoApp: App {
    var body: some Scene {
        MenuBarExtra("Corvo", systemImage: "bird") {
            Button("Sair") { NSApplication.shared.terminate(nil) }
                .keyboardShortcut("q")
        }
    }
}
```

- [ ] **Step 6: Escrever `Tests/CorvoTests/SmokeTests.swift`**

```swift
import Testing
@testable import Corvo

@Test func alvoDeTesteEstaLigado() {
    #expect(Bundle(for: BundleAnchor.self).bundleIdentifier != nil)
}

final class BundleAnchor {}
```

- [ ] **Step 7: Escrever `Makefile`**

```makefile
.PHONY: gen build test run clean

gen:
	xcodegen generate

build: gen
	xcodebuild -project Corvo.xcodeproj -scheme Corvo -configuration Debug build

test: gen
	xcodebuild test -project Corvo.xcodeproj -scheme Corvo -destination 'platform=macOS'

run: build
	open ~/Library/Developer/Xcode/DerivedData/Corvo-*/Build/Products/Debug/Corvo.app

clean:
	rm -rf Corvo.xcodeproj build DerivedData
```

- [ ] **Step 8: Gerar e compilar**

Run: `make build`
Esperado: termina com `** BUILD SUCCEEDED **`.

- [ ] **Step 9: Rodar os testes**

Run: `make test`
Esperado: `** TEST SUCCEEDED **`, 1 teste passando.

- [ ] **Step 10: Verificar o app manualmente**

Run: `make run`
Esperado: ícone de pássaro aparece na menu bar; **nenhum ícone no Dock**;
o menu abre com "Sair" e ⌘Q encerra o app.

- [ ] **Step 11: Commit**

```bash
git add project.yml Corvo-Info.plist Makefile .gitignore Sources Tests
git commit -m "feat: esqueleto do app accessory com XcodeGen e alvo de teste"
```

---

### Task 2: Banco e migrações

Entrega: banco SQLite abre em disco ou em memória, com as três tabelas criadas.

**Files:**
- Create: `Sources/Corvo/Storage/ClipItem.swift`
- Create: `Sources/Corvo/Storage/Tag.swift`
- Create: `Sources/Corvo/Storage/AppDatabase.swift`
- Create: `Tests/CorvoTests/AppDatabaseTests.swift`

**Interfaces:**
- Consumes: target da Task 1
- Produces:
  - `enum ClipKind: String, Codable { case text, image, file }`
  - `struct ClipItem` com `id: Int64?`, `kind: ClipKind`, `text: String?`,
    `blobPath: String?`, `filePath: String?`, `url: String?`,
    `sourceBundleId: String?`, `sourceName: String?`, `contentHash: String`,
    `pinned: Bool`, `createdAt: Date`, `lastUsedAt: Date?`
  - `struct Tag` com `id: Int64?`, `name: String`, `color: String?`
  - `struct ItemTag` com `itemId: Int64`, `tagId: Int64`
  - `enum AppDatabase { static func make(at url: URL?) throws -> DatabaseQueue }`
    — `nil` abre banco em memória (usado pelos testes)

- [ ] **Step 1: Escrever o teste que falha**

`Tests/CorvoTests/AppDatabaseTests.swift`:

```swift
import Testing
import GRDB
@testable import Corvo

@Test func migracaoCriaAsTresTabelas() throws {
    let dbQueue = try AppDatabase.make(at: nil)
    let tabelas = try dbQueue.read { db in
        try String.fetchAll(db, sql: """
            SELECT name FROM sqlite_master
            WHERE type = 'table' AND name NOT LIKE 'sqlite_%'
              AND name NOT LIKE 'grdb_%'
            ORDER BY name
            """)
    }
    #expect(tabelas == ["item", "itemTag", "tag"])
}

@Test func chaveEstrangeiraApagaAssociacaoJuntoComOItem() throws {
    let dbQueue = try AppDatabase.make(at: nil)
    try dbQueue.write { db in
        try db.execute(sql: """
            INSERT INTO item (kind, contentHash, pinned, createdAt)
            VALUES ('text', 'abc', 0, '2026-01-01 00:00:00.000')
            """)
        try db.execute(sql: "INSERT INTO tag (name) VALUES ('trabalho')")
        try db.execute(sql: "INSERT INTO itemTag (itemId, tagId) VALUES (1, 1)")
        try db.execute(sql: "DELETE FROM item WHERE id = 1")
        let restantes = try Int.fetchOne(db, sql: "SELECT COUNT(*) FROM itemTag")
        #expect(restantes == 0)
    }
}
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

Run: `make test`
Esperado: FALHA na compilação com `cannot find 'AppDatabase' in scope`.

- [ ] **Step 3: Escrever os records**

`Sources/Corvo/Storage/ClipItem.swift`:

```swift
import Foundation
import GRDB

enum ClipKind: String, Codable, CaseIterable, DatabaseValueConvertible {
    case text, image, file
}

struct ClipItem: Codable, Identifiable, Equatable, Hashable {
    var id: Int64?
    var kind: ClipKind
    /// Conteúdo para `.text`; nome legível para `.image` e `.file`.
    var text: String?
    /// Caminho relativo dentro do diretório de blobs, só para `.image`.
    var blobPath: String?
    /// Caminho absoluto original, só para `.file`. Não copiamos o arquivo.
    var filePath: String?
    /// `public.url` quando veio junto do conteúdo.
    var url: String?
    var sourceBundleId: String?
    var sourceName: String?
    var contentHash: String
    var pinned: Bool = false
    var createdAt: Date
    var lastUsedAt: Date?
}

extension ClipItem: FetchableRecord, MutablePersistableRecord {
    static let databaseTableName = "item"

    enum Columns {
        static let id = Column("id")
        static let kind = Column("kind")
        static let text = Column("text")
        static let sourceBundleId = Column("sourceBundleId")
        static let sourceName = Column("sourceName")
        static let contentHash = Column("contentHash")
        static let pinned = Column("pinned")
        static let createdAt = Column("createdAt")
    }

    mutating func didInsert(_ inserted: InsertionSuccess) {
        id = inserted.rowID
    }
}
```

`Sources/Corvo/Storage/Tag.swift`:

```swift
import Foundation
import GRDB

struct Tag: Codable, Identifiable, Equatable, Hashable {
    var id: Int64?
    var name: String
    var color: String?
}

extension Tag: FetchableRecord, MutablePersistableRecord {
    static let databaseTableName = "tag"

    enum Columns {
        static let id = Column("id")
        static let name = Column("name")
    }

    mutating func didInsert(_ inserted: InsertionSuccess) {
        id = inserted.rowID
    }
}

struct ItemTag: Codable, Equatable {
    var itemId: Int64
    var tagId: Int64
}

extension ItemTag: FetchableRecord, PersistableRecord {
    static let databaseTableName = "itemTag"
}
```

- [ ] **Step 4: Escrever `AppDatabase.swift`**

```swift
import Foundation
import GRDB

enum AppDatabase {
    /// Diretório de suporte do app: `~/Library/Application Support/Corvo/`.
    static var supportDirectory: URL {
        let base = FileManager.default.urls(for: .applicationSupportDirectory,
                                            in: .userDomainMask)[0]
        return base.appendingPathComponent("Corvo", isDirectory: true)
    }

    /// Abre o banco. `url == nil` abre em memória — é o que os testes usam.
    static func make(at url: URL?) throws -> DatabaseQueue {
        var config = Configuration()
        config.foreignKeysEnabled = true

        let dbQueue: DatabaseQueue
        if let url {
            try FileManager.default.createDirectory(at: url.deletingLastPathComponent(),
                                                    withIntermediateDirectories: true)
            dbQueue = try DatabaseQueue(path: url.path, configuration: config)
        } else {
            dbQueue = try DatabaseQueue(configuration: config)
        }
        try migrator.migrate(dbQueue)
        return dbQueue
    }

    static var defaultURL: URL {
        supportDirectory.appendingPathComponent("corvo.sqlite")
    }

    private static var migrator: DatabaseMigrator {
        var migrator = DatabaseMigrator()

        migrator.registerMigration("v1") { db in
            try db.create(table: "item") { t in
                t.autoIncrementedPrimaryKey("id")
                t.column("kind", .text).notNull()
                t.column("text", .text)
                t.column("blobPath", .text)
                t.column("filePath", .text)
                t.column("url", .text)
                t.column("sourceBundleId", .text)
                t.column("sourceName", .text)
                t.column("contentHash", .text).notNull()
                t.column("pinned", .boolean).notNull().defaults(to: false)
                t.column("createdAt", .datetime).notNull()
                t.column("lastUsedAt", .datetime)
            }
            // ponytail: busca é LIKE sobre `text` e `sourceName`. Com o teto de
            // 1000 itens da política de retenção isso responde em microssegundos.
            // Se o teto subir muito, trocar por tabela virtual FTS5 + triggers.
            try db.create(index: "idx_item_hash", on: "item",
                          columns: ["contentHash"], unique: true)
            try db.create(index: "idx_item_createdAt", on: "item", columns: ["createdAt"])
            try db.create(index: "idx_item_source", on: "item", columns: ["sourceBundleId"])

            try db.create(table: "tag") { t in
                t.autoIncrementedPrimaryKey("id")
                t.column("name", .text).notNull()
                t.column("color", .text)
            }
            try db.create(index: "idx_tag_name", on: "tag",
                          columns: ["name"], unique: true)

            try db.create(table: "itemTag") { t in
                t.column("itemId", .integer).notNull()
                    .references("item", onDelete: .cascade)
                t.column("tagId", .integer).notNull()
                    .references("tag", onDelete: .cascade)
                t.primaryKey(["itemId", "tagId"])
            }
        }

        return migrator
    }
}
```

- [ ] **Step 5: Rodar os testes**

Run: `make test`
Esperado: PASSA, 3 testes.

- [ ] **Step 6: Commit**

```bash
git add Sources/Corvo/Storage Tests/CorvoTests/AppDatabaseTests.swift project.yml
git commit -m "feat: schema SQLite com migrações versionadas"
```

---

### Task 3: BlobStore

Entrega: imagens gravadas em disco por hash, com coleta de órfãos.

**Files:**
- Create: `Sources/Corvo/Storage/BlobStore.swift`
- Create: `Tests/CorvoTests/BlobStoreTests.swift`

**Interfaces:**
- Consumes: `AppDatabase.supportDirectory`
- Produces:
  - `final class BlobStore`
  - `init(directory: URL)`
  - `func store(_ data: Data, hash: String, ext: String) throws -> String` —
    retorna caminho **relativo** (ex.: `"a1b2c3.png"`), idempotente por hash
  - `func url(for relativePath: String) -> URL`
  - `func collectGarbage(keeping live: Set<String>) throws -> Int` — retorna a
    quantidade de arquivos removidos

- [ ] **Step 1: Escrever o teste que falha**

`Tests/CorvoTests/BlobStoreTests.swift`:

```swift
import Foundation
import Testing
@testable import Corvo

private func tempDir() -> URL {
    let url = FileManager.default.temporaryDirectory
        .appendingPathComponent("corvo-tests-\(UUID().uuidString)")
    try? FileManager.default.createDirectory(at: url, withIntermediateDirectories: true)
    return url
}

@Test func gravaEDevolveCaminhoRelativo() throws {
    let dir = tempDir()
    defer { try? FileManager.default.removeItem(at: dir) }
    let store = BlobStore(directory: dir)

    let caminho = try store.store(Data([1, 2, 3]), hash: "abc123", ext: "png")

    #expect(caminho == "abc123.png")
    #expect(FileManager.default.fileExists(atPath: store.url(for: caminho).path))
}

@Test func gravarOMesmoHashDuasVezesNaoDuplica() throws {
    let dir = tempDir()
    defer { try? FileManager.default.removeItem(at: dir) }
    let store = BlobStore(directory: dir)

    _ = try store.store(Data([1, 2, 3]), hash: "abc123", ext: "png")
    _ = try store.store(Data([1, 2, 3]), hash: "abc123", ext: "png")

    let arquivos = try FileManager.default.contentsOfDirectory(atPath: dir.path)
    #expect(arquivos.count == 1)
}

@Test func coletaRemoveOrfaosEPreservaVivos() throws {
    let dir = tempDir()
    defer { try? FileManager.default.removeItem(at: dir) }
    let store = BlobStore(directory: dir)

    let vivo = try store.store(Data([1]), hash: "vivo", ext: "png")
    _ = try store.store(Data([2]), hash: "orfao", ext: "png")

    let removidos = try store.collectGarbage(keeping: [vivo])

    #expect(removidos == 1)
    #expect(FileManager.default.fileExists(atPath: store.url(for: vivo).path))
    let arquivos = try FileManager.default.contentsOfDirectory(atPath: dir.path)
    #expect(arquivos == ["vivo.png"])
}
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

Run: `make test`
Esperado: FALHA com `cannot find 'BlobStore' in scope`.

- [ ] **Step 3: Escrever `BlobStore.swift`**

```swift
import Foundation

/// Guarda dados binários (imagens) em disco, nomeados pelo hash do conteúdo.
/// O banco guarda só o caminho relativo — blob nunca entra em tabela.
final class BlobStore {
    private let directory: URL
    private let fm = FileManager.default

    init(directory: URL) {
        self.directory = directory
        try? fm.createDirectory(at: directory, withIntermediateDirectories: true)
    }

    /// Idempotente: gravar o mesmo hash de novo não reescreve nem duplica.
    func store(_ data: Data, hash: String, ext: String) throws -> String {
        let relativo = "\(hash).\(ext)"
        let destino = directory.appendingPathComponent(relativo)
        guard !fm.fileExists(atPath: destino.path) else { return relativo }
        try data.write(to: destino, options: .atomic)
        return relativo
    }

    func url(for relativePath: String) -> URL {
        directory.appendingPathComponent(relativePath)
    }

    /// Remove todo arquivo do diretório que não esteja em `live`.
    @discardableResult
    func collectGarbage(keeping live: Set<String>) throws -> Int {
        let existentes = try fm.contentsOfDirectory(atPath: directory.path)
        var removidos = 0
        for nome in existentes where !live.contains(nome) {
            try fm.removeItem(at: directory.appendingPathComponent(nome))
            removidos += 1
        }
        return removidos
    }
}
```

- [ ] **Step 4: Rodar os testes**

Run: `make test`
Esperado: PASSA, 6 testes.

- [ ] **Step 5: Commit**

```bash
git add Sources/Corvo/Storage/BlobStore.swift Tests/CorvoTests/BlobStoreTests.swift
git commit -m "feat: armazenamento de blobs em disco com coleta de órfãos"
```

---

### Task 4: ItemRepository — inserção, deduplicação e busca

Entrega: itens entram no banco, conteúdo repetido sobe para o topo sem perder
tags nem pin, e a busca combina texto, fonte e tag.

**Files:**
- Create: `Sources/Corvo/Storage/ItemRepository.swift`
- Create: `Tests/CorvoTests/ItemRepositoryTests.swift`

**Interfaces:**
- Consumes: `ClipItem`, `Tag`, `ItemTag`, `AppDatabase`, `BlobStore`
- Produces:
  - `struct CapturedItem` com `kind: ClipKind`, `text: String?`,
    `imageData: Data?`, `filePath: String?`, `url: String?`,
    `contentHash: String`
  - `struct ItemSource { let bundleId: String; let name: String }`
  - `struct SourceSummary { let bundleId: String; let name: String; let count: Int }`
  - `final class ItemRepository`
  - `init(dbQueue: DatabaseQueue, blobs: BlobStore)`
  - `@discardableResult func insert(_ c: CapturedItem, source: ItemSource?, now: Date) throws -> Int64`
  - `func search(text: String, sourceBundleId: String?, tagId: Int64?, limit: Int) throws -> [ClipItem]`
  - `func sources() throws -> [SourceSummary]`
  - `func allTags() throws -> [Tag]`
  - `func tags(forItem id: Int64) throws -> [Tag]`
  - `func addTag(named name: String, to itemId: Int64) throws`
  - `func removeTag(_ tagId: Int64, from itemId: Int64) throws`
  - `func setPinned(_ id: Int64, _ pinned: Bool) throws`
  - `func touch(_ id: Int64, now: Date) throws`
  - `func delete(_ id: Int64) throws`
  - `func liveBlobPaths() throws -> Set<String>`

- [ ] **Step 1: Escrever o teste que falha**

`Tests/CorvoTests/ItemRepositoryTests.swift`:

```swift
import Foundation
import Testing
import GRDB
@testable import Corvo

private func makeRepo() throws -> (ItemRepository, URL) {
    let dir = FileManager.default.temporaryDirectory
        .appendingPathComponent("corvo-repo-\(UUID().uuidString)")
    let repo = ItemRepository(dbQueue: try AppDatabase.make(at: nil),
                              blobs: BlobStore(directory: dir))
    return (repo, dir)
}

private func texto(_ s: String) -> CapturedItem {
    CapturedItem(kind: .text, text: s, imageData: nil,
                 filePath: nil, url: nil, contentHash: s)
}

private let t0 = Date(timeIntervalSince1970: 1_700_000_000)

@Test func insereEBusca() throws {
    let (repo, dir) = try makeRepo()
    defer { try? FileManager.default.removeItem(at: dir) }

    try repo.insert(texto("olá mundo"),
                    source: ItemSource(bundleId: "com.tinyspeck.slackmacgap", name: "Slack"),
                    now: t0)

    let achados = try repo.search(text: "mundo", sourceBundleId: nil, tagId: nil, limit: 50)
    #expect(achados.count == 1)
    #expect(achados[0].sourceName == "Slack")
}

@Test func conteudoRepetidoSobeAoTopoSemDuplicarNemPerderTagOuPin() throws {
    let (repo, dir) = try makeRepo()
    defer { try? FileManager.default.removeItem(at: dir) }

    let id = try repo.insert(texto("repetido"), source: nil, now: t0)
    try repo.addTag(named: "trabalho", to: id)
    try repo.setPinned(id, true)

    let idDeNovo = try repo.insert(texto("repetido"), source: nil,
                                   now: t0.addingTimeInterval(60))

    #expect(idDeNovo == id)
    let todos = try repo.search(text: "", sourceBundleId: nil, tagId: nil, limit: 50)
    #expect(todos.count == 1)
    #expect(todos[0].pinned == true)
    #expect(todos[0].createdAt == t0.addingTimeInterval(60))
    #expect(try repo.tags(forItem: id).map(\.name) == ["trabalho"])
}

@Test func buscaTextualTambemCasaONomeDaFonte() throws {
    let (repo, dir) = try makeRepo()
    defer { try? FileManager.default.removeItem(at: dir) }

    try repo.insert(texto("SELECT 1"),
                    source: ItemSource(bundleId: "com.apple.Terminal", name: "Terminal"),
                    now: t0)
    try repo.insert(texto("outra coisa"),
                    source: ItemSource(bundleId: "com.tinyspeck.slackmacgap", name: "Slack"),
                    now: t0)

    let achados = try repo.search(text: "slack", sourceBundleId: nil, tagId: nil, limit: 50)
    #expect(achados.count == 1)
    #expect(achados[0].text == "outra coisa")
}

@Test func filtrosDeFonteETagCombinamComAnd() throws {
    let (repo, dir) = try makeRepo()
    defer { try? FileManager.default.removeItem(at: dir) }

    let slack = ItemSource(bundleId: "com.tinyspeck.slackmacgap", name: "Slack")
    let a = try repo.insert(texto("a"), source: slack, now: t0)
    _ = try repo.insert(texto("b"), source: slack, now: t0)
    _ = try repo.insert(texto("c"),
                        source: ItemSource(bundleId: "com.apple.Terminal", name: "Terminal"),
                        now: t0)
    try repo.addTag(named: "importante", to: a)
    let tagId = try #require(try repo.allTags().first?.id)

    let soFonte = try repo.search(text: "", sourceBundleId: slack.bundleId,
                                  tagId: nil, limit: 50)
    #expect(soFonte.count == 2)

    let fonteETag = try repo.search(text: "", sourceBundleId: slack.bundleId,
                                    tagId: tagId, limit: 50)
    #expect(fonteETag.map(\.text) == ["a"])
}

@Test func imagemVaiParaDiscoENaoParaOBanco() throws {
    let (repo, dir) = try makeRepo()
    defer { try? FileManager.default.removeItem(at: dir) }

    let png = Data([0x89, 0x50, 0x4E, 0x47])
    let capturada = CapturedItem(kind: .image, text: "Imagem", imageData: png,
                                 filePath: nil, url: nil, contentHash: "hashpng")
    let id = try repo.insert(capturada, source: nil, now: t0)

    let item = try #require(try repo.search(text: "", sourceBundleId: nil,
                                            tagId: nil, limit: 50).first)
    #expect(item.id == id)
    #expect(item.blobPath == "hashpng.png")
    #expect(try repo.liveBlobPaths() == ["hashpng.png"])
}

@Test func resumoDeFontesContaItensPorApp() throws {
    let (repo, dir) = try makeRepo()
    defer { try? FileManager.default.removeItem(at: dir) }

    let slack = ItemSource(bundleId: "com.tinyspeck.slackmacgap", name: "Slack")
    try repo.insert(texto("a"), source: slack, now: t0)
    try repo.insert(texto("b"), source: slack, now: t0)
    try repo.insert(texto("c"),
                    source: ItemSource(bundleId: "com.apple.Terminal", name: "Terminal"),
                    now: t0)

    let fontes = try repo.sources()
    #expect(fontes.count == 2)
    #expect(fontes[0].name == "Slack")
    #expect(fontes[0].count == 2)
}
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

Run: `make test`
Esperado: FALHA com `cannot find 'ItemRepository' in scope`.

- [ ] **Step 3: Escrever `ItemRepository.swift`**

```swift
import Foundation
import GRDB

/// Conteúdo lido do pasteboard, antes de virar linha no banco.
struct CapturedItem: Equatable {
    var kind: ClipKind
    var text: String?
    var imageData: Data?
    var filePath: String?
    var url: String?
    var contentHash: String
}

struct ItemSource: Equatable {
    let bundleId: String
    let name: String
}

struct SourceSummary: Identifiable, Equatable {
    var id: String { bundleId }
    let bundleId: String
    let name: String
    let count: Int
}

final class ItemRepository {
    let dbQueue: DatabaseQueue
    private let blobs: BlobStore

    init(dbQueue: DatabaseQueue, blobs: BlobStore) {
        self.dbQueue = dbQueue
        self.blobs = blobs
    }

    /// Insere. Se o `contentHash` já existir, promove o item existente ao topo
    /// preservando tags, pin e id — recopiar não deve custar sua organização.
    @discardableResult
    func insert(_ captured: CapturedItem, source: ItemSource?, now: Date) throws -> Int64 {
        var blobPath: String?
        if let data = captured.imageData {
            blobPath = try blobs.store(data, hash: captured.contentHash, ext: "png")
        }

        return try dbQueue.write { db in
            if let existente = try ClipItem
                .filter(ClipItem.Columns.contentHash == captured.contentHash)
                .fetchOne(db), let id = existente.id {
                try db.execute(sql: "UPDATE item SET createdAt = ? WHERE id = ?",
                               arguments: [now, id])
                return id
            }

            var item = ClipItem(
                id: nil,
                kind: captured.kind,
                text: captured.text,
                blobPath: blobPath,
                filePath: captured.filePath,
                url: captured.url,
                sourceBundleId: source?.bundleId,
                sourceName: source?.name,
                contentHash: captured.contentHash,
                pinned: false,
                createdAt: now,
                lastUsedAt: nil
            )
            try item.insert(db)
            return item.id!
        }
    }

    /// `text` casa conteúdo **ou** nome da fonte, de modo que digitar "slack"
    /// filtre por origem sem passar pela barra lateral.
    func search(text: String, sourceBundleId: String?, tagId: Int64?,
                limit: Int) throws -> [ClipItem] {
        var sql = "SELECT item.* FROM item"
        var args: [DatabaseValueConvertible] = []

        if let tagId {
            sql += " JOIN itemTag ON itemTag.itemId = item.id AND itemTag.tagId = ?"
            args.append(tagId)
        }

        var condicoes: [String] = []
        let busca = text.trimmingCharacters(in: .whitespacesAndNewlines)
        if !busca.isEmpty {
            // ponytail: LIKE com scan. Ver comentário do índice em AppDatabase.
            condicoes.append("(item.text LIKE ? OR item.sourceName LIKE ?)")
            args.append("%\(busca)%")
            args.append("%\(busca)%")
        }
        if let sourceBundleId {
            condicoes.append("item.sourceBundleId = ?")
            args.append(sourceBundleId)
        }
        if !condicoes.isEmpty {
            sql += " WHERE " + condicoes.joined(separator: " AND ")
        }

        sql += " ORDER BY item.pinned DESC, item.createdAt DESC LIMIT ?"
        args.append(limit)

        return try dbQueue.read { db in
            try ClipItem.fetchAll(db, sql: sql, arguments: StatementArguments(args))
        }
    }

    func sources() throws -> [SourceSummary] {
        try dbQueue.read { db in
            try Row.fetchAll(db, sql: """
                SELECT sourceBundleId AS bundleId,
                       sourceName     AS name,
                       COUNT(*)       AS count
                FROM item
                WHERE sourceBundleId IS NOT NULL
                GROUP BY sourceBundleId
                ORDER BY count DESC, name ASC
                """)
            .map { SourceSummary(bundleId: $0["bundleId"],
                                 name: $0["name"] ?? $0["bundleId"],
                                 count: $0["count"]) }
        }
    }

    func allTags() throws -> [Tag] {
        try dbQueue.read { db in
            try Tag.order(Tag.Columns.name).fetchAll(db)
        }
    }

    func tags(forItem id: Int64) throws -> [Tag] {
        try dbQueue.read { db in
            try Tag.fetchAll(db, sql: """
                SELECT tag.* FROM tag
                JOIN itemTag ON itemTag.tagId = tag.id
                WHERE itemTag.itemId = ?
                ORDER BY tag.name
                """, arguments: [id])
        }
    }

    func addTag(named name: String, to itemId: Int64) throws {
        let limpo = name.trimmingCharacters(in: .whitespacesAndNewlines)
        guard !limpo.isEmpty else { return }
        try dbQueue.write { db in
            let existente = try Tag.filter(Tag.Columns.name == limpo).fetchOne(db)
            let tagId: Int64
            if let existente, let id = existente.id {
                tagId = id
            } else {
                var nova = Tag(id: nil, name: limpo, color: nil)
                try nova.insert(db)
                tagId = nova.id!
            }
            try ItemTag(itemId: itemId, tagId: tagId).insert(db, onConflict: .ignore)
        }
    }

    func removeTag(_ tagId: Int64, from itemId: Int64) throws {
        try dbQueue.write { db in
            try db.execute(sql: "DELETE FROM itemTag WHERE itemId = ? AND tagId = ?",
                           arguments: [itemId, tagId])
        }
    }

    func setPinned(_ id: Int64, _ pinned: Bool) throws {
        try dbQueue.write { db in
            try db.execute(sql: "UPDATE item SET pinned = ? WHERE id = ?",
                           arguments: [pinned, id])
        }
    }

    func touch(_ id: Int64, now: Date) throws {
        try dbQueue.write { db in
            try db.execute(sql: "UPDATE item SET lastUsedAt = ? WHERE id = ?",
                           arguments: [now, id])
        }
    }

    func delete(_ id: Int64) throws {
        try dbQueue.write { db in
            _ = try ClipItem.deleteOne(db, key: id)
        }
    }

    func liveBlobPaths() throws -> Set<String> {
        try dbQueue.read { db in
            Set(try String.fetchAll(db, sql:
                "SELECT blobPath FROM item WHERE blobPath IS NOT NULL"))
        }
    }
}
```

- [ ] **Step 4: Rodar os testes**

Run: `make test`
Esperado: PASSA, 12 testes.

- [ ] **Step 5: Commit**

```bash
git add Sources/Corvo/Storage/ItemRepository.swift Tests/CorvoTests/ItemRepositoryTests.swift
git commit -m "feat: repositório de itens com deduplicação por hash e busca combinada"
```

---

### Task 5: Retenção

Entrega: poda por idade e quantidade, preservando fixados e itens com tag, e
apagando os blobs que ficaram órfãos.

**Files:**
- Create: `Sources/Corvo/Storage/Retention.swift`
- Create: `Tests/CorvoTests/RetentionTests.swift`

**Interfaces:**
- Consumes: `ItemRepository`, `BlobStore`
- Produces:
  - `struct RetentionPolicy { var maxItems: Int; var maxAge: TimeInterval }`
    com `static let padrao = RetentionPolicy(maxItems: 1000, maxAge: 30 * 24 * 3600)`
  - `struct Retention { init(repo: ItemRepository, blobs: BlobStore) }`
  - `@discardableResult func prune(policy: RetentionPolicy, now: Date) throws -> Int`

- [ ] **Step 1: Escrever o teste que falha**

`Tests/CorvoTests/RetentionTests.swift`:

```swift
import Foundation
import Testing
@testable import Corvo

private let agora = Date(timeIntervalSince1970: 1_700_000_000)

private func ambiente() throws -> (ItemRepository, BlobStore, Retention, URL) {
    let dir = FileManager.default.temporaryDirectory
        .appendingPathComponent("corvo-ret-\(UUID().uuidString)")
    let blobs = BlobStore(directory: dir)
    let repo = ItemRepository(dbQueue: try AppDatabase.make(at: nil), blobs: blobs)
    return (repo, blobs, Retention(repo: repo, blobs: blobs), dir)
}

private func texto(_ s: String) -> CapturedItem {
    CapturedItem(kind: .text, text: s, imageData: nil,
                 filePath: nil, url: nil, contentHash: s)
}

@Test func podaPorIdadeRemoveOsAntigos() throws {
    let (repo, _, retencao, dir) = try ambiente()
    defer { try? FileManager.default.removeItem(at: dir) }

    try repo.insert(texto("velho"), source: nil, now: agora.addingTimeInterval(-40 * 86400))
    try repo.insert(texto("novo"), source: nil, now: agora)

    let removidos = try retencao.prune(policy: .padrao, now: agora)

    #expect(removidos == 1)
    let restantes = try repo.search(text: "", sourceBundleId: nil, tagId: nil, limit: 50)
    #expect(restantes.map(\.text) == ["novo"])
}

@Test func fixadosEItensComTagNuncaExpiram() throws {
    let (repo, _, retencao, dir) = try ambiente()
    defer { try? FileManager.default.removeItem(at: dir) }

    let antigo = agora.addingTimeInterval(-40 * 86400)
    let fixado = try repo.insert(texto("fixado"), source: nil, now: antigo)
    let comTag = try repo.insert(texto("com tag"), source: nil, now: antigo)
    try repo.insert(texto("descartável"), source: nil, now: antigo)
    try repo.setPinned(fixado, true)
    try repo.addTag(named: "guardar", to: comTag)

    let removidos = try retencao.prune(policy: .padrao, now: agora)

    #expect(removidos == 1)
    let restantes = try repo.search(text: "", sourceBundleId: nil, tagId: nil, limit: 50)
    #expect(Set(restantes.compactMap(\.text)) == ["fixado", "com tag"])
}

@Test func podaPorQuantidadeNaoContaProtegidos() throws {
    let (repo, _, retencao, dir) = try ambiente()
    defer { try? FileManager.default.removeItem(at: dir) }

    let fixado = try repo.insert(texto("fixado"), source: nil, now: agora)
    try repo.setPinned(fixado, true)
    for i in 0..<5 {
        try repo.insert(texto("item \(i)"), source: nil,
                        now: agora.addingTimeInterval(Double(i)))
    }

    let politica = RetentionPolicy(maxItems: 3, maxAge: .greatestFiniteMagnitude)
    let removidos = try retencao.prune(policy: politica, now: agora)

    // 5 desprotegidos, teto 3 → remove os 2 mais antigos. Fixado permanece.
    #expect(removidos == 2)
    let restantes = try repo.search(text: "", sourceBundleId: nil, tagId: nil, limit: 50)
    #expect(restantes.count == 4)
}

@Test func podaApagaBlobsQueFicaramOrfaos() throws {
    let (repo, blobs, retencao, dir) = try ambiente()
    defer { try? FileManager.default.removeItem(at: dir) }

    let png = Data([0x89, 0x50])
    try repo.insert(CapturedItem(kind: .image, text: "img", imageData: png,
                                 filePath: nil, url: nil, contentHash: "velha"),
                    source: nil, now: agora.addingTimeInterval(-40 * 86400))

    #expect(FileManager.default.fileExists(atPath: blobs.url(for: "velha.png").path))

    try retencao.prune(policy: .padrao, now: agora)

    #expect(!FileManager.default.fileExists(atPath: blobs.url(for: "velha.png").path))
}
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

Run: `make test`
Esperado: FALHA com `cannot find 'Retention' in scope`.

- [ ] **Step 3: Escrever `Retention.swift`**

```swift
import Foundation
import GRDB

struct RetentionPolicy: Equatable {
    var maxItems: Int
    var maxAge: TimeInterval

    static let padrao = RetentionPolicy(maxItems: 1000, maxAge: 30 * 24 * 3600)
}

/// Poda o histórico. Item fixado ou com ao menos uma tag é protegido: nunca
/// expira e não conta para o teto de quantidade.
struct Retention {
    private let repo: ItemRepository
    private let blobs: BlobStore

    init(repo: ItemRepository, blobs: BlobStore) {
        self.repo = repo
        self.blobs = blobs
    }

    @discardableResult
    func prune(policy: RetentionPolicy, now: Date) throws -> Int {
        let protegidos = """
            item.pinned = 1
            OR EXISTS (SELECT 1 FROM itemTag WHERE itemTag.itemId = item.id)
            """

        let removidos = try repo.dbQueue.write { db -> Int in
            var total = 0

            if policy.maxAge.isFinite {
                let corte = now.addingTimeInterval(-policy.maxAge)
                try db.execute(sql: """
                    DELETE FROM item
                    WHERE createdAt < ? AND NOT (\(protegidos))
                    """, arguments: [corte])
                total += db.changesCount
            }

            try db.execute(sql: """
                DELETE FROM item WHERE id IN (
                    SELECT id FROM item
                    WHERE NOT (\(protegidos))
                    ORDER BY createdAt DESC
                    LIMIT -1 OFFSET ?
                )
                """, arguments: [policy.maxItems])
            total += db.changesCount

            return total
        }

        try blobs.collectGarbage(keeping: repo.liveBlobPaths())
        return removidos
    }
}
```

- [ ] **Step 4: Rodar os testes**

Run: `make test`
Esperado: PASSA, 16 testes.

- [ ] **Step 5: Commit**

```bash
git add Sources/Corvo/Storage/Retention.swift Tests/CorvoTests/RetentionTests.swift
git commit -m "feat: política de retenção preservando fixados e itens com tag"
```

---

### Task 6: SourceTracker — heurística do app de origem

Entrega: dado o histórico de ativações de apps, a captura é creditada ao app
correto mesmo quando o usuário troca de app logo depois do ⌘C.

**Files:**
- Create: `Sources/Corvo/Clipboard/SourceTracker.swift`
- Create: `Tests/CorvoTests/SourceTrackerTests.swift`

**Interfaces:**
- Consumes: `ItemSource`
- Produces:
  - `final class SourceTracker`
  - `init(janela: TimeInterval = 0.3)`
  - `func registrarAtivacao(_ source: ItemSource, at: Date)`
  - `func fonteDaCaptura(at: Date) -> ItemSource?` — se a última troca de app
    ocorreu há menos de `janela`, devolve o app **anterior**; senão, o atual
  - `func começarAObservarOSistema()` — assina
    `NSWorkspace.didActivateApplicationNotification`
  - `var appEmFoco: NSRunningApplication?` — guardado antes do painel abrir

- [ ] **Step 1: Escrever o teste que falha**

`Tests/CorvoTests/SourceTrackerTests.swift`:

```swift
import Foundation
import Testing
@testable import Corvo

private let slack = ItemSource(bundleId: "com.tinyspeck.slackmacgap", name: "Slack")
private let terminal = ItemSource(bundleId: "com.apple.Terminal", name: "Terminal")
private let t0 = Date(timeIntervalSince1970: 1_700_000_000)

@Test func semAtivacaoRegistradaNaoHaFonte() {
    let tracker = SourceTracker()
    #expect(tracker.fonteDaCaptura(at: t0) == nil)
}

@Test func trocaRecenteCreditaOAppAnterior() {
    let tracker = SourceTracker(janela: 0.3)
    tracker.registrarAtivacao(slack, at: t0)
    tracker.registrarAtivacao(terminal, at: t0.addingTimeInterval(1))

    // Copiou no Slack e trocou pro Terminal 0,1s antes do poll.
    let fonte = tracker.fonteDaCaptura(at: t0.addingTimeInterval(1.1))

    #expect(fonte == slack)
}

@Test func trocaAntigaCreditaOAppAtual() {
    let tracker = SourceTracker(janela: 0.3)
    tracker.registrarAtivacao(slack, at: t0)
    tracker.registrarAtivacao(terminal, at: t0.addingTimeInterval(1))

    let fonte = tracker.fonteDaCaptura(at: t0.addingTimeInterval(5))

    #expect(fonte == terminal)
}

@Test func semAppAnteriorATrocaRecenteCreditaOAtual() {
    let tracker = SourceTracker(janela: 0.3)
    tracker.registrarAtivacao(slack, at: t0)

    #expect(tracker.fonteDaCaptura(at: t0.addingTimeInterval(0.1)) == slack)
}
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

Run: `make test`
Esperado: FALHA com `cannot find 'SourceTracker' in scope`.

- [ ] **Step 3: Escrever `SourceTracker.swift`**

```swift
import AppKit

/// Descobre de qual app veio o conteúdo copiado.
///
/// Duas fontes, nesta ordem: a convenção `org.nspasteboard.source` (tratada no
/// PasteboardMonitor, autoritativa quando presente) e, na falta dela, o app em
/// foco no momento da captura.
///
/// O poll roda a cada 0,3s, então entre o ⌘C e a leitura o usuário pode ter
/// trocado de app. Guardamos a ativação anterior com timestamp: se a troca
/// aconteceu dentro da janela, o crédito vai para o app de antes.
@MainActor
final class SourceTracker {
    private struct Ativacao {
        let source: ItemSource
        let quando: Date
    }

    private let janela: TimeInterval
    private var atual: Ativacao?
    private var anterior: Ativacao?

    /// App que estava em foco antes do painel do Corvo aparecer. É para ele que
    /// o Paster devolve o foco na hora de colar.
    private(set) var appEmFoco: NSRunningApplication?

    init(janela: TimeInterval = 0.3) {
        self.janela = janela
    }

    func registrarAtivacao(_ source: ItemSource, at quando: Date) {
        anterior = atual
        atual = Ativacao(source: source, quando: quando)
    }

    func fonteDaCaptura(at quando: Date) -> ItemSource? {
        guard let atual else { return nil }
        guard quando.timeIntervalSince(atual.quando) < janela,
              let anterior else { return atual.source }
        return anterior.source
    }

    func começarAObservarOSistema() {
        let ws = NSWorkspace.shared
        if let app = ws.frontmostApplication, let source = Self.fonte(de: app) {
            registrarAtivacao(source, at: Date())
        }
        ws.notificationCenter.addObserver(
            forName: NSWorkspace.didActivateApplicationNotification,
            object: nil, queue: .main
        ) { [weak self] note in
            guard let app = note.userInfo?[NSWorkspace.applicationUserInfoKey]
                    as? NSRunningApplication else { return }
            MainActor.assumeIsolated {
                guard let self else { return }
                if app.bundleIdentifier != Bundle.main.bundleIdentifier {
                    self.appEmFoco = app
                }
                guard let source = Self.fonte(de: app) else { return }
                self.registrarAtivacao(source, at: Date())
            }
        }
    }

    private static func fonte(de app: NSRunningApplication) -> ItemSource? {
        guard let bundleId = app.bundleIdentifier,
              bundleId != Bundle.main.bundleIdentifier else { return nil }
        return ItemSource(bundleId: bundleId, name: app.localizedName ?? bundleId)
    }
}
```

- [ ] **Step 4: Rodar os testes**

Run: `make test`
Esperado: PASSA, 20 testes.

- [ ] **Step 5: Commit**

```bash
git add Sources/Corvo/Clipboard/SourceTracker.swift Tests/CorvoTests/SourceTrackerTests.swift
git commit -m "feat: heurística de app de origem com janela de troca de foco"
```

---

### Task 7: PasteboardMonitor — poll e filtro de privacidade

Entrega: o poller detecta mudanças no pasteboard, descarta conteúdo sensível e
grava o resto.

**Files:**
- Create: `Sources/Corvo/Clipboard/PasteboardReading.swift`
- Create: `Sources/Corvo/Clipboard/PasteboardMonitor.swift`
- Create: `Tests/CorvoTests/PasteboardMonitorTests.swift`

**Interfaces:**
- Consumes: `CapturedItem`, `ItemRepository`, `SourceTracker`
- Produces:
  - `protocol PasteboardReading` com `var changeCount: Int { get }`,
    `var tipos: [NSPasteboard.PasteboardType] { get }` — **não** pode se chamar
    `types`: `NSPasteboard.types` já existe e é opcional, redeclarar dá
    `invalid redeclaration`,
    `func data(forType: NSPasteboard.PasteboardType) -> Data?`,
    `func string(forType: NSPasteboard.PasteboardType) -> String?`,
    `func fileURLs() -> [URL]`
  - `extension NSPasteboard: PasteboardReading`
  - `final class PasteboardMonitor`
  - `init(pasteboard: PasteboardReading, repo: ItemRepository, tracker: SourceTracker, prefs: Preferences)`
  - `func poll(now: Date) throws` — um ciclo, chamável direto no teste
  - `func start()` / `func stop()` — `Timer` de 0,3s
  - `static func hash(of data: Data) -> String`

- [ ] **Step 1: Escrever o teste que falha**

`Tests/CorvoTests/PasteboardMonitorTests.swift`:

```swift
import AppKit
import Foundation
import Testing
@testable import Corvo

final class PasteboardFalso: PasteboardReading, @unchecked Sendable {
    var changeCount = 0
    var tipos: [NSPasteboard.PasteboardType] = []
    var strings: [NSPasteboard.PasteboardType: String] = [:]
    var datas: [NSPasteboard.PasteboardType: Data] = [:]
    var urls: [URL] = []

    func data(forType t: NSPasteboard.PasteboardType) -> Data? { datas[t] }
    func string(forType t: NSPasteboard.PasteboardType) -> String? { strings[t] }
    func fileURLs() -> [URL] { urls }

    func copiarTexto(_ s: String) {
        changeCount += 1
        tipos = [.string]
        strings = [.string: s]
        datas = [:]
        urls = []
    }
}

private func ambiente() throws -> (PasteboardFalso, ItemRepository, PasteboardMonitor, URL) {
    let dir = FileManager.default.temporaryDirectory
        .appendingPathComponent("corvo-mon-\(UUID().uuidString)")
    let repo = ItemRepository(dbQueue: try AppDatabase.make(at: nil),
                              blobs: BlobStore(directory: dir))
    let pb = PasteboardFalso()
    let monitor = PasteboardMonitor(pasteboard: pb, repo: repo,
                                    tracker: SourceTracker(),
                                    prefs: Preferences(defaults: UserDefaults(
                                        suiteName: UUID().uuidString)!))
    return (pb, repo, monitor, dir)
}

private let t0 = Date(timeIntervalSince1970: 1_700_000_000)

@MainActor @Test func capturaTextoQuandoOChangeCountMuda() throws {
    let (pb, repo, monitor, dir) = try ambiente()
    defer { try? FileManager.default.removeItem(at: dir) }

    pb.copiarTexto("olá")
    try monitor.poll(now: t0)

    let itens = try repo.search(text: "", sourceBundleId: nil, tagId: nil, limit: 50)
    #expect(itens.map(\.text) == ["olá"])
}

@MainActor @Test func naoRecapturaSeOChangeCountNaoMudou() throws {
    let (pb, repo, monitor, dir) = try ambiente()
    defer { try? FileManager.default.removeItem(at: dir) }

    pb.copiarTexto("olá")
    try monitor.poll(now: t0)
    try monitor.poll(now: t0.addingTimeInterval(0.3))

    let itens = try repo.search(text: "", sourceBundleId: nil, tagId: nil, limit: 50)
    #expect(itens.count == 1)
}

@MainActor @Test func descartaConteudoMarcadoComoSigiloso() throws {
    let (pb, repo, monitor, dir) = try ambiente()
    defer { try? FileManager.default.removeItem(at: dir) }

    pb.copiarTexto("senha-secreta")
    pb.tipos.append(.init(rawValue: "org.nspasteboard.ConcealedType"))
    try monitor.poll(now: t0)

    let itens = try repo.search(text: "", sourceBundleId: nil, tagId: nil, limit: 50)
    #expect(itens.isEmpty)
}

@MainActor @Test func descartaConteudoTransitorio() throws {
    let (pb, repo, monitor, dir) = try ambiente()
    defer { try? FileManager.default.removeItem(at: dir) }

    pb.copiarTexto("temporário")
    pb.tipos.append(.init(rawValue: "org.nspasteboard.TransientType"))
    try monitor.poll(now: t0)

    #expect(try repo.search(text: "", sourceBundleId: nil, tagId: nil, limit: 50).isEmpty)
}

@MainActor @Test func descartaConteudoDeAppNaBlocklist() throws {
    let (pb, repo, monitor, dir) = try ambiente()
    defer { try? FileManager.default.removeItem(at: dir) }

    monitor.prefs.blocklist = ["com.apple.keychainaccess"]
    pb.copiarTexto("segredo")
    pb.tipos.append(.init(rawValue: "org.nspasteboard.source"))
    pb.strings[.init(rawValue: "org.nspasteboard.source")] = "com.apple.keychainaccess"
    try monitor.poll(now: t0)

    #expect(try repo.search(text: "", sourceBundleId: nil, tagId: nil, limit: 50).isEmpty)
}

@MainActor @Test func descartaTextoVazio() throws {
    let (pb, repo, monitor, dir) = try ambiente()
    defer { try? FileManager.default.removeItem(at: dir) }

    pb.copiarTexto("   \n  ")
    try monitor.poll(now: t0)

    #expect(try repo.search(text: "", sourceBundleId: nil, tagId: nil, limit: 50).isEmpty)
}
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

Run: `make test`
Esperado: FALHA com `cannot find 'PasteboardMonitor' in scope`.

- [ ] **Step 3: Escrever `PasteboardReading.swift`**

```swift
import AppKit

/// Só o que o monitor precisa do pasteboard. Existe para o teste poder
/// substituir o `NSPasteboard` real, que é estado global do sistema.
///
/// A propriedade se chama `tipos`, não `types`: `NSPasteboard.types` já existe
/// (e é opcional), então redeclarar na extensão daria `invalid redeclaration`.
protocol PasteboardReading: AnyObject {
    var changeCount: Int { get }
    var tipos: [NSPasteboard.PasteboardType] { get }
    func data(forType type: NSPasteboard.PasteboardType) -> Data?
    func string(forType type: NSPasteboard.PasteboardType) -> String?
    func fileURLs() -> [URL]
}

extension NSPasteboard: PasteboardReading {
    var tipos: [NSPasteboard.PasteboardType] {
        pasteboardItems?.first?.types ?? []
    }

    func fileURLs() -> [URL] {
        readObjects(forClasses: [NSURL.self],
                    options: [.urlReadingFileURLsOnly: true]) as? [URL] ?? []
    }
}
```

- [ ] **Step 4: Escrever `PasteboardMonitor.swift`**

```swift
import AppKit
import CryptoKit
import Foundation

@MainActor
final class PasteboardMonitor {
    static let concealed = NSPasteboard.PasteboardType("org.nspasteboard.ConcealedType")
    static let transient = NSPasteboard.PasteboardType("org.nspasteboard.TransientType")
    static let sourceType = NSPasteboard.PasteboardType("org.nspasteboard.source")

    private let pasteboard: PasteboardReading
    private let repo: ItemRepository
    private let tracker: SourceTracker
    let prefs: Preferences

    private var ultimoChangeCount: Int
    private var timer: Timer?

    init(pasteboard: PasteboardReading, repo: ItemRepository,
         tracker: SourceTracker, prefs: Preferences) {
        self.pasteboard = pasteboard
        self.repo = repo
        self.tracker = tracker
        self.prefs = prefs
        self.ultimoChangeCount = pasteboard.changeCount
    }

    func start() {
        // ponytail: NSPasteboard não notifica mudanças — poll do changeCount é a
        // única via. Todo gerenciador de clipboard do macOS faz assim.
        let t = Timer(timeInterval: 0.3, repeats: true) { [weak self] _ in
            MainActor.assumeIsolated {
                try? self?.poll(now: Date())
            }
        }
        RunLoop.main.add(t, forMode: .common)
        timer = t
    }

    func stop() {
        timer?.invalidate()
        timer = nil
    }

    func poll(now: Date) throws {
        let atual = pasteboard.changeCount
        guard atual != ultimoChangeCount else { return }
        ultimoChangeCount = atual

        let tipos = Set(pasteboard.tipos)

        // Trust boundary: gerenciadores de senha marcam o conteúdo. Descartamos
        // antes de qualquer gravação, sem passar por disco nem por banco.
        guard !tipos.contains(Self.concealed), !tipos.contains(Self.transient) else { return }

        let declarada = pasteboard.string(forType: Self.sourceType)
            .map { ItemSource(bundleId: $0, name: $0) }
        let fonte = declarada ?? tracker.fonteDaCaptura(at: now)

        if let bundleId = fonte?.bundleId, prefs.blocklist.contains(bundleId) { return }

        guard let capturado = capturar(tipos: tipos) else { return }
        try repo.insert(capturado, source: fonte, now: now)
    }

    private func capturar(tipos: Set<NSPasteboard.PasteboardType>) -> CapturedItem? {
        let url = pasteboard.string(forType: .URL)

        let arquivos = pasteboard.fileURLs()
        if let arquivo = arquivos.first {
            return CapturedItem(kind: .file, text: arquivo.lastPathComponent,
                                imageData: nil, filePath: arquivo.path, url: url,
                                contentHash: Self.hash(of: Data(arquivo.path.utf8)))
        }

        if tipos.contains(.tiff) || tipos.contains(.png) {
            let tipo: NSPasteboard.PasteboardType = tipos.contains(.png) ? .png : .tiff
            guard let bruto = pasteboard.data(forType: tipo),
                  let png = Self.paraPNG(bruto) else { return nil }
            return CapturedItem(kind: .image, text: "Imagem", imageData: png,
                                filePath: nil, url: url,
                                contentHash: Self.hash(of: png))
        }

        guard let texto = pasteboard.string(forType: .string),
              !texto.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty
        else { return nil }
        return CapturedItem(kind: .text, text: texto, imageData: nil,
                            filePath: nil, url: url,
                            contentHash: Self.hash(of: Data(texto.utf8)))
    }

    private static func paraPNG(_ dados: Data) -> Data? {
        guard let rep = NSBitmapImageRep(data: dados) else { return dados }
        return rep.representation(using: .png, properties: [:])
    }

    static func hash(of data: Data) -> String {
        SHA256.hash(data: data).map { String(format: "%02x", $0) }.joined()
    }
}
```

- [ ] **Step 5: Escrever `Preferences.swift` (necessário para compilar)**

`Sources/Corvo/System/Preferences.swift`:

```swift
import Foundation

/// `UserDefaults` tipado. Um lugar só para tudo que o usuário configura.
final class Preferences {
    private let defaults: UserDefaults

    init(defaults: UserDefaults = .standard) {
        self.defaults = defaults
    }

    var blocklist: [String] {
        get { defaults.stringArray(forKey: "blocklist") ?? [] }
        set { defaults.set(newValue, forKey: "blocklist") }
    }

    var maxItems: Int {
        get {
            let v = defaults.integer(forKey: "maxItems")
            return v > 0 ? v : RetentionPolicy.padrao.maxItems
        }
        set { defaults.set(newValue, forKey: "maxItems") }
    }

    var maxAgeDias: Int {
        get {
            let v = defaults.integer(forKey: "maxAgeDias")
            return v > 0 ? v : 30
        }
        set { defaults.set(newValue, forKey: "maxAgeDias") }
    }

    var retentionPolicy: RetentionPolicy {
        RetentionPolicy(maxItems: maxItems, maxAge: Double(maxAgeDias) * 86400)
    }
}
```

- [ ] **Step 6: Rodar os testes**

Run: `make test`
Esperado: PASSA, 26 testes.

- [ ] **Step 7: Commit**

```bash
git add Sources/Corvo/Clipboard Sources/Corvo/System/Preferences.swift \
        Tests/CorvoTests/PasteboardMonitorTests.swift
git commit -m "feat: poller do pasteboard com filtro de privacidade"
```

---

### Task 8: Atalho global e painel flutuante

Entrega: ⌘⇧V abre e fecha um painel flutuante vazio; Esc fecha.

**Files:**
- Create: `Sources/Corvo/System/GlobalHotkey.swift`
- Create: `Sources/Corvo/UI/FloatingPanel.swift`
- Modify: `Sources/Corvo/App/CorvoApp.swift`
- Create: `Sources/Corvo/App/AppEnvironment.swift`

**Interfaces:**
- Consumes: tudo das tasks 2–7
- Produces:
  - `final class GlobalHotkey { init?(keyCode: UInt32, modifiers: UInt32, handler: @escaping () -> Void) }`
  - `enum HotkeyCodes { static let v: UInt32; static let cmdShift: UInt32 }`
  - `final class FloatingPanel: NSPanel` com `override var canBecomeKey: Bool { true }`
  - `final class PanelController` com `init(content: some View)`, `func alternar()`,
    `func mostrar()`, `func esconder()`
  - `@MainActor final class AppEnvironment` com `let repo`, `let blobs`, `let prefs`,
    `let tracker`, `let monitor`, `let retention`, `func iniciar()`

Sem teste unitário: o valor está em janela real e evento de sistema real, que
teste unitário não observa. Verificação manual nos steps 6 e 7.

- [ ] **Step 1: Escrever `GlobalHotkey.swift`**

```swift
import Carbon.HIToolbox
import Foundation

enum HotkeyCodes {
    static let v = UInt32(kVK_ANSI_V)
    static let cmdShift = UInt32(cmdKey | shiftKey)
}

// ponytail: dicionário global porque o callback do Carbon é ponteiro de função
// C e não captura contexto. Carbon só chama na main thread, daí o
// `nonisolated(unsafe)`. Se um dia precisarmos de atalho configurável, trocar
// o arquivo inteiro pelo pacote KeyboardShortcuts, que já traz o gravador.
nonisolated(unsafe) private var handlersDeAtalho: [UInt32: () -> Void] = [:]
nonisolated(unsafe) private var proximoId: UInt32 = 1

private func despachar(_ next: EventHandlerCallRef?, _ event: EventRef?,
                       _ userData: UnsafeMutableRawPointer?) -> OSStatus {
    var id = EventHotKeyID()
    let status = GetEventParameter(event, EventParamName(kEventParamDirectObject),
                                   EventParamType(typeEventHotKeyID), nil,
                                   MemoryLayout<EventHotKeyID>.size, nil, &id)
    guard status == noErr else { return status }
    handlersDeAtalho[id.id]?()
    return noErr
}

/// Registra um atalho global. Enquanto a instância viver, o atalho existe.
final class GlobalHotkey {
    private var ref: EventHotKeyRef?
    private let id: UInt32

    init?(keyCode: UInt32, modifiers: UInt32, handler: @escaping () -> Void) {
        id = proximoId
        proximoId += 1

        var tipo = EventTypeSpec(eventClass: OSType(kEventClassKeyboard),
                                 eventKind: UInt32(kEventHotKeyPressed))
        InstallEventHandler(GetApplicationEventTarget(), despachar, 1, &tipo, nil, nil)

        let hotKeyID = EventHotKeyID(signature: OSType(0x43525630), id: id) // 'CRV0'
        let status = RegisterEventHotKey(keyCode, modifiers, hotKeyID,
                                         GetApplicationEventTarget(), 0, &ref)
        guard status == noErr else { return nil }
        handlersDeAtalho[id] = handler
    }

    deinit {
        if let ref { UnregisterEventHotKey(ref) }
        handlersDeAtalho[id] = nil
    }
}
```

- [ ] **Step 2: Escrever `FloatingPanel.swift`**

```swift
import AppKit
import SwiftUI

/// `NSPanel` precisa da sobrescrita de `canBecomeKey` para o campo de busca
/// receber foco — painel comum não vira key window.
final class FloatingPanel: NSPanel {
    override var canBecomeKey: Bool { true }
    override var canBecomeMain: Bool { false }

    override func cancelOperation(_ sender: Any?) {
        orderOut(nil)   // Esc fecha
    }
}

@MainActor
final class PanelController {
    private let panel: FloatingPanel

    init(content: some View) {
        panel = FloatingPanel(
            contentRect: NSRect(x: 0, y: 0, width: 900, height: 420),
            styleMask: [.titled, .closable, .resizable,
                        .fullSizeContentView, .nonactivatingPanel],
            backing: .buffered, defer: false)
        panel.titleVisibility = .hidden
        panel.titlebarAppearsTransparent = true
        panel.isMovableByWindowBackground = true
        panel.level = .floating
        panel.hidesOnDeactivate = true
        panel.collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary]
        panel.isReleasedWhenClosed = false
        panel.contentView = NSHostingView(rootView: content)
    }

    var estaVisivel: Bool { panel.isVisible }

    func alternar() { panel.isVisible ? esconder() : mostrar() }

    func mostrar() {
        panel.center()
        NSApp.activate(ignoringOtherApps: true)
        panel.makeKeyAndOrderFront(nil)
    }

    func esconder() { panel.orderOut(nil) }
}
```

- [ ] **Step 3: Escrever `AppEnvironment.swift`**

```swift
import AppKit
import Foundation

/// Monta e conecta as dependências. Único lugar que conhece todas as camadas.
@MainActor
final class AppEnvironment {
    let prefs: Preferences
    let blobs: BlobStore
    let repo: ItemRepository
    let tracker: SourceTracker
    let retention: Retention
    private(set) var monitor: PasteboardMonitor!
    private var podaTimer: Timer?

    init() throws {
        prefs = Preferences()
        blobs = BlobStore(directory: AppDatabase.supportDirectory
            .appendingPathComponent("blobs", isDirectory: true))
        repo = ItemRepository(dbQueue: try AppDatabase.make(at: AppDatabase.defaultURL),
                              blobs: blobs)
        tracker = SourceTracker()
        retention = Retention(repo: repo, blobs: blobs)
        monitor = PasteboardMonitor(pasteboard: NSPasteboard.general, repo: repo,
                                    tracker: tracker, prefs: prefs)
    }

    func iniciar() {
        tracker.começarAObservarOSistema()
        monitor.start()
        podar()
        let t = Timer(timeInterval: 3600, repeats: true) { [weak self] _ in
            MainActor.assumeIsolated { self?.podar() }
        }
        RunLoop.main.add(t, forMode: .common)
        podaTimer = t
    }

    private func podar() {
        try? retention.prune(policy: prefs.retentionPolicy, now: Date())
    }
}
```

- [ ] **Step 4: Reescrever `CorvoApp.swift`**

```swift
import AppKit
import SwiftUI

@main
struct CorvoApp: App {
    @NSApplicationDelegateAdaptor(AppDelegate.self) private var delegate

    var body: some Scene {
        MenuBarExtra("Corvo", systemImage: "bird") {
            Button("Mostrar histórico") { delegate.painel?.mostrar() }
                .keyboardShortcut("v", modifiers: [.command, .shift])
            Divider()
            Button("Sair") { NSApplication.shared.terminate(nil) }
                .keyboardShortcut("q")
        }
    }
}

@MainActor
final class AppDelegate: NSObject, NSApplicationDelegate {
    private(set) var env: AppEnvironment?
    private(set) var painel: PanelController?
    private var atalho: GlobalHotkey?

    func applicationDidFinishLaunching(_ notification: Notification) {
        do {
            let env = try AppEnvironment()
            env.iniciar()
            self.env = env
            painel = PanelController(content: Text("histórico entra aqui")
                .frame(maxWidth: .infinity, maxHeight: .infinity))
        } catch {
            NSAlert(error: error).runModal()
            NSApp.terminate(nil)
            return
        }

        atalho = GlobalHotkey(keyCode: HotkeyCodes.v,
                              modifiers: HotkeyCodes.cmdShift) { [weak self] in
            MainActor.assumeIsolated { self?.painel?.alternar() }
        }
        if atalho == nil {
            NSLog("Corvo: não foi possível registrar ⌘⇧V — atalho já em uso?")
        }
    }
}
```

- [ ] **Step 5: Compilar**

Run: `make build`
Esperado: `** BUILD SUCCEEDED **`.

- [ ] **Step 6: Verificar o atalho manualmente**

Run: `make run`, depois apertar `⌘⇧V` com o Finder em foco.
Esperado: painel flutuante aparece centralizado com o texto placeholder; `⌘⇧V`
de novo esconde; `Esc` esconde; o painel fica acima das outras janelas.

- [ ] **Step 7: Verificar que a captura está gravando**

Copie três textos de apps diferentes, depois rode:

```bash
sqlite3 ~/Library/Application\ Support/Corvo/corvo.sqlite \
  "SELECT kind, sourceName, substr(text,1,30) FROM item ORDER BY createdAt DESC;"
```

Esperado: três linhas, cada uma com o `sourceName` do app correto.

- [ ] **Step 8: Commit**

```bash
git add Sources/Corvo/System/GlobalHotkey.swift Sources/Corvo/UI/FloatingPanel.swift \
        Sources/Corvo/App
git commit -m "feat: atalho global ⌘⇧V e painel flutuante"
```

---

### Task 9: Modelo de tela do histórico

Entrega: um `@Observable` que expõe a lista filtrada e reage a mudanças no banco
sem refresh manual.

**Files:**
- Create: `Sources/Corvo/UI/HistoryModel.swift`
- Create: `Tests/CorvoTests/HistoryModelTests.swift`

**Interfaces:**
- Consumes: `ItemRepository`, `SourceSummary`, `Tag`, `ClipItem`
- Produces:
  - `@Observable @MainActor final class HistoryModel`
  - `init(repo: ItemRepository)`
  - `var busca: String`, `var fonteSelecionada: String?`, `var tagSelecionada: Int64?`
  - `private(set) var itens: [ClipItem]`, `var fontes: [SourceSummary]`, `var tags: [Tag]`
  - `var indiceSelecionado: Int`
  - `var itemSelecionado: ClipItem?`
  - `func recarregar()`
  - `func observarBanco()`
  - `func mover(_ passo: Int)`

- [ ] **Step 1: Escrever o teste que falha**

`Tests/CorvoTests/HistoryModelTests.swift`:

```swift
import Foundation
import Testing
@testable import Corvo

private let t0 = Date(timeIntervalSince1970: 1_700_000_000)

@MainActor
private func modelo() throws -> (HistoryModel, ItemRepository, URL) {
    let dir = FileManager.default.temporaryDirectory
        .appendingPathComponent("corvo-hm-\(UUID().uuidString)")
    let repo = ItemRepository(dbQueue: try AppDatabase.make(at: nil),
                              blobs: BlobStore(directory: dir))
    return (HistoryModel(repo: repo), repo, dir)
}

private func texto(_ s: String) -> CapturedItem {
    CapturedItem(kind: .text, text: s, imageData: nil,
                 filePath: nil, url: nil, contentHash: s)
}

@MainActor @Test func recarregarTrazOsItensMaisNovosPrimeiro() throws {
    let (model, repo, dir) = try modelo()
    defer { try? FileManager.default.removeItem(at: dir) }

    try repo.insert(texto("antigo"), source: nil, now: t0)
    try repo.insert(texto("recente"), source: nil, now: t0.addingTimeInterval(10))
    model.recarregar()

    #expect(model.itens.map(\.text) == ["recente", "antigo"])
}

@MainActor @Test func buscaFiltraERessetaASelecao() throws {
    let (model, repo, dir) = try modelo()
    defer { try? FileManager.default.removeItem(at: dir) }

    try repo.insert(texto("alfa"), source: nil, now: t0)
    try repo.insert(texto("beta"), source: nil, now: t0.addingTimeInterval(1))
    model.recarregar()
    model.indiceSelecionado = 1

    model.busca = "alfa"
    model.recarregar()

    #expect(model.itens.map(\.text) == ["alfa"])
    #expect(model.indiceSelecionado == 0)
}

@MainActor @Test func moverNaoSaiDosLimites() throws {
    let (model, repo, dir) = try modelo()
    defer { try? FileManager.default.removeItem(at: dir) }

    try repo.insert(texto("a"), source: nil, now: t0)
    try repo.insert(texto("b"), source: nil, now: t0.addingTimeInterval(1))
    model.recarregar()

    model.mover(-1)
    #expect(model.indiceSelecionado == 0)
    model.mover(1)
    model.mover(1)
    model.mover(1)
    #expect(model.indiceSelecionado == 1)
}

@MainActor @Test func listaDeFontesAcompanhaOsItens() throws {
    let (model, repo, dir) = try modelo()
    defer { try? FileManager.default.removeItem(at: dir) }

    try repo.insert(texto("a"), source: ItemSource(bundleId: "com.apple.Terminal",
                                                   name: "Terminal"), now: t0)
    model.recarregar()

    #expect(model.fontes.map(\.name) == ["Terminal"])
}
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

Run: `make test`
Esperado: FALHA com `cannot find 'HistoryModel' in scope`.

- [ ] **Step 3: Escrever `HistoryModel.swift`**

```swift
import Foundation
import GRDB
import Observation

@Observable
@MainActor
final class HistoryModel {
    private let repo: ItemRepository
    private var cancelavel: AnyDatabaseCancellable?

    var busca: String = "" { didSet { recarregar() } }
    var fonteSelecionada: String? { didSet { recarregar() } }
    var tagSelecionada: Int64? { didSet { recarregar() } }

    private(set) var itens: [ClipItem] = []
    private(set) var fontes: [SourceSummary] = []
    private(set) var tags: [Tag] = []
    var indiceSelecionado: Int = 0

    var itemSelecionado: ClipItem? {
        itens.indices.contains(indiceSelecionado) ? itens[indiceSelecionado] : nil
    }

    init(repo: ItemRepository) {
        self.repo = repo
        recarregar()
    }

    func recarregar() {
        itens = (try? repo.search(text: busca, sourceBundleId: fonteSelecionada,
                                  tagId: tagSelecionada, limit: 200)) ?? []
        fontes = (try? repo.sources()) ?? []
        tags = (try? repo.allTags()) ?? []
        indiceSelecionado = min(indiceSelecionado, max(itens.count - 1, 0))
        if itens.isEmpty { indiceSelecionado = 0 }
    }

    /// Faz a lista reagir ao poller sem refresh manual.
    func observarBanco() {
        cancelavel = ValueObservation
            .tracking { db in try Int.fetchOne(db, sql: "SELECT COUNT(*) FROM item") ?? 0 }
            .start(in: repo.dbQueue, scheduling: .async(onQueue: .main),
                   onError: { _ in },
                   onChange: { [weak self] _ in
                       // `.async(onQueue: .main)` garante a main thread; o
                       // compilador não sabe disso sozinho.
                       MainActor.assumeIsolated { self?.recarregar() }
                   })
    }

    func mover(_ passo: Int) {
        guard !itens.isEmpty else { return }
        indiceSelecionado = min(max(indiceSelecionado + passo, 0), itens.count - 1)
    }

    func tags(de item: ClipItem) -> [Tag] {
        guard let id = item.id else { return [] }
        return (try? repo.tags(forItem: id)) ?? []
    }

    func adicionarTag(_ nome: String, a item: ClipItem) {
        guard let id = item.id else { return }
        try? repo.addTag(named: nome, to: id)
        recarregar()
    }

    func alternarFixado(_ item: ClipItem) {
        guard let id = item.id else { return }
        try? repo.setPinned(id, !item.pinned)
        recarregar()
    }

    func apagar(_ item: ClipItem) {
        guard let id = item.id else { return }
        try? repo.delete(id)
        recarregar()
    }

    func marcarUso(_ item: ClipItem) {
        guard let id = item.id else { return }
        try? repo.touch(id, now: Date())
    }
}
```

- [ ] **Step 4: Rodar os testes**

Run: `make test`
Esperado: PASSA, 30 testes.

- [ ] **Step 5: Commit**

```bash
git add Sources/Corvo/UI/HistoryModel.swift Tests/CorvoTests/HistoryModelTests.swift
git commit -m "feat: modelo de tela do histórico com observação do banco"
```

---

### Task 10: A tela do histórico

Entrega: o painel mostra busca, barra lateral de fontes e tags, e o carrossel de
cards com ícone do app de origem. Navegação por teclado funciona.

**REQUIRED SUB-SKILLS antes de escrever qualquer view:** invoque
`ui-ux-pro-max:ui-ux-pro-max` e `frontend-design:frontend-design`. Esta é a tela
onde o app ganha ou perde — a referência declarada no spec é o Paste. Não escreva
SwiftUI antes de rodar as duas skills.

**Files:**
- Create: `Sources/Corvo/UI/ItemCard.swift`
- Create: `Sources/Corvo/UI/FilterSidebar.swift`
- Create: `Sources/Corvo/UI/HistoryView.swift`
- Create: `Sources/Corvo/UI/AppIcon.swift`
- Modify: `Sources/Corvo/App/CorvoApp.swift` (trocar o placeholder pela view real)

**Interfaces:**
- Consumes: `HistoryModel`, `ClipItem`, `SourceSummary`, `Tag`, `BlobStore`
- Produces:
  - `enum AppIcon { static func imagem(paraBundleId: String?) -> NSImage? }` —
    resolve via `NSWorkspace.shared.urlForApplication(withBundleIdentifier:)`,
    com cache em memória
  - `struct ItemCard: View { let item: ClipItem; let tags: [Tag]; let selecionado: Bool; let blobs: BlobStore }`
  - `struct FilterSidebar: View { @Bindable var model: HistoryModel }`
  - `struct HistoryView: View { @Bindable var model: HistoryModel; let blobs: BlobStore; let aoColar: (ClipItem) -> Void }`

- [ ] **Step 1: Invocar as skills de design**

Rode `ui-ux-pro-max:ui-ux-pro-max` e `frontend-design:frontend-design` com o
contexto: painel flutuante macOS 900×420, carrossel horizontal de cards, campo de
busca no topo, barra lateral de filtros à esquerda, tema claro e escuro,
referência visual Paste.app. Aplique o resultado nos steps seguintes.

- [ ] **Step 2: Escrever `AppIcon.swift`**

```swift
import AppKit

/// Ícone do app de origem. Resolvido pelo bundle ID e mantido em cache — a
/// lista redesenha muito e ler o ícone do disco a cada frame é desperdício.
enum AppIcon {
    nonisolated(unsafe) private static var cache: [String: NSImage] = [:]

    @MainActor
    static func imagem(paraBundleId bundleId: String?) -> NSImage? {
        guard let bundleId else { return nil }
        if let cacheado = cache[bundleId] { return cacheado }
        guard let url = NSWorkspace.shared
            .urlForApplication(withBundleIdentifier: bundleId) else { return nil }
        let icone = NSWorkspace.shared.icon(forFile: url.path)
        cache[bundleId] = icone
        return icone
    }
}
```

- [ ] **Step 3: Escrever `ItemCard.swift`**

Aplique aqui a direção visual das skills do Step 1. O card **precisa** conter,
independentemente do estilo escolhido: preview do conteúdo (texto truncado,
thumbnail para imagem, ícone de documento + nome para arquivo), ícone do app de
origem, as tags do item, e indicação visual de seleção e de fixado. Arquivo cujo
`filePath` não existe mais aparece esmaecido com o rótulo "indisponível".

```swift
import AppKit
import SwiftUI

struct ItemCard: View {
    let item: ClipItem
    let tags: [Tag]
    let selecionado: Bool
    let blobs: BlobStore

    private var arquivoSumiu: Bool {
        guard item.kind == .file, let caminho = item.filePath else { return false }
        return !FileManager.default.fileExists(atPath: caminho)
    }

    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            HStack(spacing: 6) {
                if let icone = AppIcon.imagem(paraBundleId: item.sourceBundleId) {
                    Image(nsImage: icone).resizable().frame(width: 14, height: 14)
                }
                Text(item.sourceName ?? "Desconhecido")
                    .font(.caption2)
                    .foregroundStyle(.secondary)
                Spacer()
                if item.pinned {
                    Image(systemName: "pin.fill").font(.caption2)
                }
            }

            preview

            if !tags.isEmpty {
                HStack(spacing: 4) {
                    ForEach(tags) { tag in
                        Text(tag.name)
                            .font(.caption2)
                            .padding(.horizontal, 6).padding(.vertical, 2)
                            .background(.quaternary, in: Capsule())
                    }
                }
            }
        }
        .padding(12)
        .frame(width: 200, height: 240, alignment: .topLeading)
        .background(.regularMaterial, in: RoundedRectangle(cornerRadius: 12))
        .overlay {
            RoundedRectangle(cornerRadius: 12)
                .strokeBorder(selecionado ? Color.accentColor : .clear, lineWidth: 2)
        }
        .opacity(arquivoSumiu ? 0.45 : 1)
    }

    @ViewBuilder
    private var preview: some View {
        switch item.kind {
        case .text:
            Text(item.text ?? "")
                .font(.callout)
                .lineLimit(8)
                .frame(maxWidth: .infinity, alignment: .leading)
        case .image:
            if let caminho = item.blobPath,
               let img = NSImage(contentsOf: blobs.url(for: caminho)) {
                Image(nsImage: img)
                    .resizable().scaledToFit()
                    .frame(maxWidth: .infinity, maxHeight: 150)
            } else {
                Label("Imagem indisponível", systemImage: "photo")
                    .font(.caption)
            }
        case .file:
            VStack(alignment: .leading, spacing: 4) {
                Image(systemName: "doc").font(.largeTitle)
                Text(item.text ?? "").font(.callout).lineLimit(2)
                if arquivoSumiu {
                    Text("indisponível").font(.caption2).foregroundStyle(.secondary)
                }
            }
        }
        Spacer(minLength: 0)
    }
}
```

- [ ] **Step 4: Escrever `FilterSidebar.swift`**

```swift
import SwiftUI

struct FilterSidebar: View {
    @Bindable var model: HistoryModel

    var body: some View {
        List {
            Section("Fontes") {
                ForEach(model.fontes) { fonte in
                    Button {
                        model.fonteSelecionada =
                            model.fonteSelecionada == fonte.bundleId ? nil : fonte.bundleId
                    } label: {
                        HStack(spacing: 6) {
                            if let icone = AppIcon.imagem(paraBundleId: fonte.bundleId) {
                                Image(nsImage: icone).resizable().frame(width: 16, height: 16)
                            }
                            Text(fonte.name).lineLimit(1)
                            Spacer()
                            Text("\(fonte.count)")
                                .font(.caption).foregroundStyle(.secondary)
                        }
                    }
                    .buttonStyle(.plain)
                    .fontWeight(model.fonteSelecionada == fonte.bundleId ? .bold : .regular)
                }
            }

            Section("Tags") {
                ForEach(model.tags) { tag in
                    Button {
                        model.tagSelecionada = model.tagSelecionada == tag.id ? nil : tag.id
                    } label: {
                        Label(tag.name, systemImage: "tag")
                    }
                    .buttonStyle(.plain)
                    .fontWeight(model.tagSelecionada == tag.id ? .bold : .regular)
                }
            }
        }
        .listStyle(.sidebar)
        .frame(width: 190)
    }
}
```

- [ ] **Step 5: Escrever `HistoryView.swift`**

```swift
import SwiftUI

struct HistoryView: View {
    @Bindable var model: HistoryModel
    let blobs: BlobStore
    let aoColar: (ClipItem) -> Void

    @State private var editandoTag = false
    @State private var textoDaTag = ""
    @FocusState private var buscaFocada: Bool

    var body: some View {
        HStack(spacing: 0) {
            FilterSidebar(model: model)
            Divider()
            VStack(spacing: 0) {
                busca
                Divider()
                carrossel
            }
        }
        .frame(minWidth: 700, minHeight: 380)
        .onAppear { buscaFocada = true }
        .sheet(isPresented: $editandoTag) { folhaDeTag }
    }

    private var busca: some View {
        HStack {
            Image(systemName: "magnifyingglass").foregroundStyle(.secondary)
            TextField("Buscar por conteúdo ou app…", text: $model.busca)
                .textFieldStyle(.plain)
                .focused($buscaFocada)
                .onSubmit { colarSelecionado() }
        }
        .padding(12)
    }

    @ViewBuilder
    private var carrossel: some View {
        if model.itens.isEmpty {
            ContentUnavailableView("Nada por aqui",
                                   systemImage: "bird",
                                   description: Text("Copie algo e volte."))
        } else {
            ScrollViewReader { proxy in
                ScrollView(.horizontal) {
                    HStack(spacing: 12) {
                        ForEach(Array(model.itens.enumerated()), id: \.element.id) { i, item in
                            ItemCard(item: item, tags: model.tags(de: item),
                                     selecionado: i == model.indiceSelecionado,
                                     blobs: blobs)
                                .id(item.id)
                                .onTapGesture(count: 2) { aoColar(item) }
                                .onTapGesture { model.indiceSelecionado = i }
                        }
                    }
                    .padding(16)
                }
                .onChange(of: model.indiceSelecionado) { _, novo in
                    withAnimation { proxy.scrollTo(model.itens[safe: novo]?.id) }
                }
            }
            .background(atalhos)
        }
    }

    private var atalhos: some View {
        Group {
            Button("") { model.mover(-1) }.keyboardShortcut(.leftArrow, modifiers: [])
            Button("") { model.mover(1) }.keyboardShortcut(.rightArrow, modifiers: [])
            Button("") { colarSelecionado() }.keyboardShortcut(.return, modifiers: [])
            Button("") { if let i = model.itemSelecionado { model.apagar(i) } }
                .keyboardShortcut(.delete, modifiers: [])
            Button("") { if let i = model.itemSelecionado { model.alternarFixado(i) } }
                .keyboardShortcut("p", modifiers: .command)
            Button("") { textoDaTag = ""; editandoTag = true }
                .keyboardShortcut("t", modifiers: .command)
        }
        .opacity(0).frame(width: 0, height: 0)
    }

    private var folhaDeTag: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Nova tag").font(.headline)
            TextField("nome da tag", text: $textoDaTag)
                .onSubmit { confirmarTag() }
            HStack {
                Spacer()
                Button("Cancelar") { editandoTag = false }
                Button("Adicionar") { confirmarTag() }.keyboardShortcut(.defaultAction)
            }
        }
        .padding(20)
        .frame(width: 300)
    }

    private func confirmarTag() {
        if let item = model.itemSelecionado {
            model.adicionarTag(textoDaTag, a: item)
        }
        editandoTag = false
    }

    private func colarSelecionado() {
        guard let item = model.itemSelecionado else { return }
        aoColar(item)
    }
}

extension Array {
    subscript(safe index: Int) -> Element? {
        indices.contains(index) ? self[index] : nil
    }
}
```

- [ ] **Step 6: Ligar a view no `AppDelegate`**

Em `Sources/Corvo/App/CorvoApp.swift`, substitua a criação do `PanelController`
dentro de `applicationDidFinishLaunching`:

```swift
            let model = HistoryModel(repo: env.repo)
            model.observarBanco()
            self.model = model
            painel = PanelController(content: HistoryView(
                model: model,
                blobs: env.blobs,
                aoColar: { [weak self] item in self?.colar(item) }
            ))
```

E adicione à classe `AppDelegate`:

```swift
    private(set) var model: HistoryModel?

    private func colar(_ item: ClipItem) {
        painel?.esconder()
        model?.marcarUso(item)
        // Task 11 substitui esta linha pela colagem de verdade.
        NSLog("Corvo: colar item \(item.id ?? -1)")
    }
```

- [ ] **Step 7: Compilar e rodar os testes**

Run: `make test`
Esperado: PASSA, 30 testes.

- [ ] **Step 8: Verificar a tela manualmente**

Run: `make run`. Copie um texto do Terminal, uma imagem (⌃⇧⌘4) e um arquivo do
Finder. Aperte `⌘⇧V`.

Esperado: três cards, cada um com o ícone do app correto; digitar "terminal" na
busca deixa só o do Terminal; ←/→ movem a seleção e o carrossel acompanha;
clicar numa fonte na barra lateral filtra e clicar de novo desfaz; `⌘T` adiciona
tag e ela aparece no card e na barra lateral; `⌘P` fixa e o item vai para o
início; `Delete` apaga.

- [ ] **Step 9: Commit**

```bash
git add Sources/Corvo/UI Sources/Corvo/App/CorvoApp.swift
git commit -m "feat: tela do histórico com carrossel, busca e filtros"
```

---

### Task 11: Colar de volta

Entrega: Enter num card escreve no clipboard, devolve o foco ao app anterior e
posta ⌘V. Sem permissão de Acessibilidade, degrada para só copiar e avisa.

**Files:**
- Create: `Sources/Corvo/Clipboard/Paster.swift`
- Modify: `Sources/Corvo/App/CorvoApp.swift` (método `colar`)

**Interfaces:**
- Consumes: `ClipItem`, `BlobStore`, `SourceTracker.appEmFoco`
- Produces:
  - `enum Paster`
  - `static var temPermissao: Bool`
  - `static func pedirPermissao()`
  - `static func escreverNoClipboard(_ item: ClipItem, blobs: BlobStore)`
  - `static func colar(_ item: ClipItem, blobs: BlobStore, em app: NSRunningApplication?) -> Bool`
    — `false` quando faltou permissão (só copiou)
  - `static func abrirAjustesDeAcessibilidade()`

Sem teste unitário: postar eventos de teclado no sistema e o estado da permissão
de Acessibilidade não são observáveis em teste unitário. Verificação manual nos
steps 4 e 5.

- [ ] **Step 1: Escrever `Paster.swift`**

```swift
import AppKit
import ApplicationServices
import Carbon.HIToolbox

enum Paster {
    static var temPermissao: Bool { AXIsProcessTrusted() }

    static func pedirPermissao() {
        let chave = kAXTrustedCheckOptionPrompt.takeUnretainedValue()
        _ = AXIsProcessTrustedWithOptions([chave: true] as CFDictionary)
    }

    static func abrirAjustesDeAcessibilidade() {
        let url = URL(string: "x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility")!
        NSWorkspace.shared.open(url)
    }

    static func escreverNoClipboard(_ item: ClipItem, blobs: BlobStore) {
        let pb = NSPasteboard.general
        pb.clearContents()

        switch item.kind {
        case .text:
            pb.setString(item.text ?? "", forType: .string)
        case .image:
            guard let caminho = item.blobPath,
                  let img = NSImage(contentsOf: blobs.url(for: caminho)) else { return }
            pb.writeObjects([img])
        case .file:
            guard let caminho = item.filePath,
                  FileManager.default.fileExists(atPath: caminho) else { return }
            pb.writeObjects([URL(fileURLWithPath: caminho) as NSURL])
        }
    }

    /// Escreve no clipboard e cola no app indicado. Devolve `false` se faltou
    /// permissão de Acessibilidade — nesse caso o conteúdo foi só copiado.
    @discardableResult
    static func colar(_ item: ClipItem, blobs: BlobStore,
                      em app: NSRunningApplication?) -> Bool {
        escreverNoClipboard(item, blobs: blobs)
        guard temPermissao else { return false }

        app?.activate()
        // O ⌘V precisa chegar depois de o app voltar ao foco; sem o atraso ele
        // é entregue à janela que está saindo.
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.12) {
            postarCmdV()
        }
        return true
    }

    private static func postarCmdV() {
        let fonte = CGEventSource(stateID: .combinedSessionState)
        let v = CGKeyCode(kVK_ANSI_V)
        guard let baixo = CGEvent(keyboardEventSource: fonte, virtualKey: v, keyDown: true),
              let cima = CGEvent(keyboardEventSource: fonte, virtualKey: v, keyDown: false)
        else { return }
        baixo.flags = .maskCommand
        cima.flags = .maskCommand
        baixo.post(tap: .cgAnnotatedSessionEventTap)
        cima.post(tap: .cgAnnotatedSessionEventTap)
    }
}
```

- [ ] **Step 2: Ligar no `AppDelegate`**

Substitua o método `colar` de `Sources/Corvo/App/CorvoApp.swift`:

```swift
    private func colar(_ item: ClipItem) {
        guard let env else { return }
        painel?.esconder()
        model?.marcarUso(item)
        let colou = Paster.colar(item, blobs: env.blobs, em: env.tracker.appEmFoco)
        guard !colou else { return }
        avisarFaltaDePermissao()
    }

    private func avisarFaltaDePermissao() {
        let alerta = NSAlert()
        alerta.messageText = "Copiado, mas não colado"
        alerta.informativeText = """
            O Corvo precisa da permissão de Acessibilidade para colar direto no \
            app onde você estava. O conteúdo já está na sua área de transferência \
            — cole com ⌘V.
            """
        alerta.addButton(withTitle: "Abrir Ajustes")
        alerta.addButton(withTitle: "Agora não")
        if alerta.runModal() == .alertFirstButtonReturn {
            Paster.abrirAjustesDeAcessibilidade()
        }
    }
```

- [ ] **Step 3: Compilar**

Run: `make build`
Esperado: `** BUILD SUCCEEDED **`.

- [ ] **Step 4: Verificar o caminho sem permissão**

Garanta que o Corvo **não** está em Ajustes do Sistema → Privacidade e
Segurança → Acessibilidade. Rode `make run`, copie um texto, abra o TextEdit,
aperte `⌘⇧V` e Enter num card.

Esperado: painel fecha, alerta "Copiado, mas não colado" aparece, e "Abrir
Ajustes" leva ao painel de Acessibilidade. `⌘V` manual cola o conteúdo certo.

- [ ] **Step 5: Verificar o caminho com permissão**

Conceda a permissão ao Corvo em Ajustes → Acessibilidade, reinicie o app com
`make run`. Repita o teste no TextEdit.

Esperado: painel fecha e o texto aparece no TextEdit sozinho, sem alerta. Repita
com uma imagem e com um arquivo do Finder colado numa pasta.

- [ ] **Step 6: Commit**

```bash
git add Sources/Corvo/Clipboard/Paster.swift Sources/Corvo/App/CorvoApp.swift
git commit -m "feat: colar de volta no app anterior com degradação sem Acessibilidade"
```

---

### Task 12: Preferências e iniciar com o sistema

Entrega: janela de preferências com login item, limites de retenção, blocklist e
estado da permissão de Acessibilidade.

**Files:**
- Create: `Sources/Corvo/System/LoginItem.swift`
- Create: `Sources/Corvo/UI/PreferencesView.swift`
- Modify: `Sources/Corvo/App/CorvoApp.swift` (item de menu + cena `Settings`)

**Interfaces:**
- Consumes: `Preferences`, `Paster`
- Produces:
  - `enum LoginItem { static var ativo: Bool { get }; static func definir(_ ativo: Bool) throws }`
  - `struct PreferencesView: View { let prefs: Preferences }`

- [ ] **Step 1: Escrever `LoginItem.swift`**

```swift
import ServiceManagement

/// Registro no "Itens de Início de Sessão" dos Ajustes do Sistema.
enum LoginItem {
    static var ativo: Bool { SMAppService.mainApp.status == .enabled }

    static func definir(_ ativo: Bool) throws {
        if ativo {
            try SMAppService.mainApp.register()
            return
        }
        try SMAppService.mainApp.unregister()
    }
}
```

- [ ] **Step 2: Escrever `PreferencesView.swift`**

```swift
import SwiftUI

struct PreferencesView: View {
    let prefs: Preferences

    @State private var iniciarComSistema = LoginItem.ativo
    @State private var maxItems: Int
    @State private var maxAgeDias: Int
    @State private var blocklist: String
    @State private var erro: String?

    init(prefs: Preferences) {
        self.prefs = prefs
        _maxItems = State(initialValue: prefs.maxItems)
        _maxAgeDias = State(initialValue: prefs.maxAgeDias)
        _blocklist = State(initialValue: prefs.blocklist.joined(separator: "\n"))
    }

    var body: some View {
        Form {
            Section {
                Toggle("Iniciar com o sistema", isOn: $iniciarComSistema)
                    .onChange(of: iniciarComSistema) { _, novo in
                        do { try LoginItem.definir(novo) }
                        catch {
                            erro = error.localizedDescription
                            iniciarComSistema = LoginItem.ativo
                        }
                    }
            }

            Section("Retenção") {
                TextField("Máximo de itens", value: $maxItems, format: .number)
                    .onChange(of: maxItems) { _, novo in prefs.maxItems = novo }
                TextField("Apagar após (dias)", value: $maxAgeDias, format: .number)
                    .onChange(of: maxAgeDias) { _, novo in prefs.maxAgeDias = novo }
                Text("Itens fixados ou com tag nunca expiram.")
                    .font(.caption).foregroundStyle(.secondary)
            }

            Section("Apps ignorados") {
                TextEditor(text: $blocklist)
                    .frame(height: 80)
                    .font(.system(.body, design: .monospaced))
                    .onChange(of: blocklist) { _, novo in
                        prefs.blocklist = novo
                            .split(separator: "\n")
                            .map { $0.trimmingCharacters(in: .whitespaces) }
                            .filter { !$0.isEmpty }
                    }
                Text("Um bundle ID por linha. Nada copiado nesses apps é guardado.")
                    .font(.caption).foregroundStyle(.secondary)
            }

            Section("Permissões") {
                HStack {
                    Label(Paster.temPermissao ? "Acessibilidade concedida"
                                              : "Acessibilidade pendente",
                          systemImage: Paster.temPermissao ? "checkmark.circle.fill"
                                                           : "exclamationmark.triangle.fill")
                    Spacer()
                    Button("Abrir Ajustes") { Paster.abrirAjustesDeAcessibilidade() }
                }
                Text("Sem ela, o Corvo copia mas não cola sozinho.")
                    .font(.caption).foregroundStyle(.secondary)
            }
        }
        .formStyle(.grouped)
        .frame(width: 420)
        .alert("Não foi possível alterar", isPresented: .constant(erro != nil)) {
            Button("OK") { erro = nil }
        } message: {
            Text(erro ?? "")
        }
    }
}
```

- [ ] **Step 3: Adicionar a cena de preferências**

Em `Sources/Corvo/App/CorvoApp.swift`, dentro de `body`, depois do `MenuBarExtra`:

```swift
        Settings {
            if let prefs = delegate.env?.prefs {
                PreferencesView(prefs: prefs)
            }
        }
```

E dentro do `MenuBarExtra`, antes do `Divider()`:

```swift
            SettingsLink { Text("Preferências…") }
                .keyboardShortcut(",")
```

- [ ] **Step 4: Compilar e rodar os testes**

Run: `make test`
Esperado: PASSA, 30 testes.

- [ ] **Step 5: Verificar manualmente**

Run: `make run`. Abra Preferências pelo menu da barra.

Esperado: ligar "Iniciar com o sistema" faz o Corvo aparecer em Ajustes do
Sistema → Geral → Itens de Início de Sessão; desligar remove. Mudar o máximo de
itens para 5, copiar 8 textos e esperar a próxima poda deixa 5. Adicionar
`com.apple.Terminal` à blocklist faz cópias do Terminal pararem de aparecer.

- [ ] **Step 6: Commit**

```bash
git add Sources/Corvo/System/LoginItem.swift Sources/Corvo/UI/PreferencesView.swift \
        Sources/Corvo/App/CorvoApp.swift
git commit -m "feat: preferências com login item, retenção e blocklist"
```

---

### Task 13: Testes de integração com AppKit real

Entrega: a costura entre nosso código e o `NSPasteboard` de verdade é exercitada
por testes, não só o `PasteboardFalso`.

Motivo: as tasks 2–9 testam a nossa lógica contra dublês. O adaptador
`NSPasteboard: PasteboardReading` — conversão TIFF→PNG, leitura de URLs de
arquivo, lista de tipos do primeiro item — não é tocado por nenhum deles. Um bug
ali deixa os 30 testes verdes e o app sem capturar nada.
`NSPasteboard(name:)` cria um pasteboard privado, real e isolado do sistema:
dá para exercitar o caminho verdadeiro sem sujar a área de transferência do
usuário nem depender de permissão.

**Files:**
- Modify: `Sources/Corvo/Clipboard/Paster.swift` (injeção do pasteboard)
- Create: `Tests/CorvoTests/IntegracaoTests.swift`

**Interfaces:**
- Consumes: tudo das tasks 2–11
- Produces: `Paster.escreverNoClipboard(_:blobs:em:)` passa a aceitar
  `em pb: NSPasteboard = .general`

- [ ] **Step 1: Tornar o pasteboard do Paster injetável**

Em `Sources/Corvo/Clipboard/Paster.swift`, troque a assinatura e a primeira
linha de `escreverNoClipboard`:

```swift
    static func escreverNoClipboard(_ item: ClipItem, blobs: BlobStore,
                                    em pb: NSPasteboard = .general) {
        pb.clearContents()
```

E remova a linha `let pb = NSPasteboard.general` que existia logo abaixo. O resto
do corpo do método fica igual. Em `colar(_:blobs:em:)`, a chamada
`escreverNoClipboard(item, blobs: blobs)` continua valendo pelo valor padrão.

- [ ] **Step 2: Escrever os testes de integração**

`Tests/CorvoTests/IntegracaoTests.swift`:

```swift
import AppKit
import Foundation
import Testing
@testable import Corvo

private let t0 = Date(timeIntervalSince1970: 1_700_000_000)

/// Pasteboard real, porém privado: exercita o `NSPasteboard` de verdade sem
/// tocar na área de transferência do usuário.
private func pasteboardPrivado() -> NSPasteboard {
    let pb = NSPasteboard(name: NSPasteboard.Name(rawValue: "corvo.test.\(UUID().uuidString)"))
    pb.clearContents()
    return pb
}

@MainActor
private func ambiente(_ pb: NSPasteboard) throws -> (PasteboardMonitor, ItemRepository, BlobStore, URL) {
    let dir = FileManager.default.temporaryDirectory
        .appendingPathComponent("corvo-int-\(UUID().uuidString)")
    let blobs = BlobStore(directory: dir)
    let repo = ItemRepository(dbQueue: try AppDatabase.make(at: nil), blobs: blobs)
    let monitor = PasteboardMonitor(
        pasteboard: pb, repo: repo, tracker: SourceTracker(),
        prefs: Preferences(defaults: UserDefaults(suiteName: UUID().uuidString)!))
    return (monitor, repo, blobs, dir)
}

@MainActor @Test func capturaTextoDeUmNSPasteboardReal() throws {
    let pb = pasteboardPrivado()
    let (monitor, repo, _, dir) = try ambiente(pb)
    defer { try? FileManager.default.removeItem(at: dir) }

    pb.clearContents()
    pb.setString("texto de verdade", forType: .string)
    try monitor.poll(now: t0)

    let itens = try repo.search(text: "", sourceBundleId: nil, tagId: nil, limit: 10)
    #expect(itens.map(\.text) == ["texto de verdade"])
    #expect(itens.first?.kind == .text)
}

@MainActor @Test func capturaImagemDeUmNSPasteboardRealEGravaPNGLegivel() throws {
    let pb = pasteboardPrivado()
    let (monitor, repo, blobs, dir) = try ambiente(pb)
    defer { try? FileManager.default.removeItem(at: dir) }

    let imagem = NSImage(size: NSSize(width: 8, height: 8))
    imagem.lockFocus()
    NSColor.systemRed.setFill()
    NSRect(x: 0, y: 0, width: 8, height: 8).fill()
    imagem.unlockFocus()

    pb.clearContents()
    pb.writeObjects([imagem])
    try monitor.poll(now: t0)

    let item = try #require(try repo.search(text: "", sourceBundleId: nil,
                                            tagId: nil, limit: 10).first)
    #expect(item.kind == .image)
    let caminho = try #require(item.blobPath)
    let regravada = try #require(NSImage(contentsOf: blobs.url(for: caminho)))
    #expect(regravada.size.width == 8)
}

@MainActor @Test func capturaArquivoDeUmNSPasteboardRealGuardandoOCaminho() throws {
    let pb = pasteboardPrivado()
    let (monitor, repo, _, dir) = try ambiente(pb)
    defer { try? FileManager.default.removeItem(at: dir) }

    let arquivo = dir.appendingPathComponent("nota.txt")
    try FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true)
    try Data("oi".utf8).write(to: arquivo)

    pb.clearContents()
    pb.writeObjects([arquivo as NSURL])
    try monitor.poll(now: t0)

    let item = try #require(try repo.search(text: "", sourceBundleId: nil,
                                            tagId: nil, limit: 10).first)
    #expect(item.kind == .file)
    #expect(item.filePath == arquivo.path)
    #expect(item.text == "nota.txt")
}

@MainActor @Test func pasteboardRealMarcadoComoSigilosoNaoEGravado() throws {
    let pb = pasteboardPrivado()
    let (monitor, repo, _, dir) = try ambiente(pb)
    defer { try? FileManager.default.removeItem(at: dir) }

    pb.clearContents()
    let item = NSPasteboardItem()
    item.setString("senha", forType: .string)
    item.setString("", forType: .init(rawValue: "org.nspasteboard.ConcealedType"))
    pb.writeObjects([item])
    try monitor.poll(now: t0)

    #expect(try repo.search(text: "", sourceBundleId: nil, tagId: nil, limit: 10).isEmpty)
}

@MainActor @Test func voltaAoPasteboardOMesmoConteudoQueFoiCapturado() throws {
    let origem = pasteboardPrivado()
    let destino = pasteboardPrivado()
    let (monitor, repo, blobs, dir) = try ambiente(origem)
    defer { try? FileManager.default.removeItem(at: dir) }

    origem.clearContents()
    origem.setString("ida e volta", forType: .string)
    try monitor.poll(now: t0)

    let item = try #require(try repo.search(text: "", sourceBundleId: nil,
                                            tagId: nil, limit: 10).first)
    Paster.escreverNoClipboard(item, blobs: blobs, em: destino)

    #expect(destino.string(forType: .string) == "ida e volta")
}

@MainActor @Test func fluxoCompletoDoPollerAteAListaDaTela() throws {
    let pb = pasteboardPrivado()
    let (monitor, repo, _, dir) = try ambiente(pb)
    defer { try? FileManager.default.removeItem(at: dir) }

    let model = HistoryModel(repo: repo)

    pb.clearContents()
    pb.setString("primeiro", forType: .string)
    try monitor.poll(now: t0)

    pb.clearContents()
    pb.setString("segundo", forType: .string)
    try monitor.poll(now: t0.addingTimeInterval(1))

    model.recarregar()
    #expect(model.itens.map(\.text) == ["segundo", "primeiro"])

    model.busca = "prim"
    #expect(model.itens.map(\.text) == ["primeiro"])
}

@Test func bancoEmDiscoPersisteEAMigracaoEIdempotente() throws {
    let dir = FileManager.default.temporaryDirectory
        .appendingPathComponent("corvo-disco-\(UUID().uuidString)")
    defer { try? FileManager.default.removeItem(at: dir) }
    let url = dir.appendingPathComponent("corvo.sqlite")

    do {
        let repo = ItemRepository(dbQueue: try AppDatabase.make(at: url),
                                  blobs: BlobStore(directory: dir))
        try repo.insert(CapturedItem(kind: .text, text: "sobrevivi", imageData: nil,
                                     filePath: nil, url: nil, contentHash: "h1"),
                        source: nil, now: t0)
    }

    // Reabrir roda o migrator de novo: não pode explodir nem perder dado.
    let repo = ItemRepository(dbQueue: try AppDatabase.make(at: url),
                              blobs: BlobStore(directory: dir))
    let itens = try repo.search(text: "", sourceBundleId: nil, tagId: nil, limit: 10)
    #expect(itens.map(\.text) == ["sobrevivi"])
}
```

- [ ] **Step 3: Rodar os testes**

Run: `make test`
Esperado: PASSA, 37 testes. Se algum falhar, o bug está no adaptador real do
`NSPasteboard` ou no `Paster` — não nos testes.

- [ ] **Step 4: Commit**

```bash
git add Sources/Corvo/Clipboard/Paster.swift Tests/CorvoTests/IntegracaoTests.swift
git commit -m "test: integração com NSPasteboard real e persistência em disco"
```

---

### Task 14: Empacotamento e distribuição

Entrega: release automatizado no GitHub com `.zip` do app, README, licença e o
cask do Homebrew.

**Files:**
- Create: `README.md`
- Create: `LICENSE`
- Create: `.github/workflows/release.yml`
- Create: `Casks/corvo.rb`
- Modify: `Makefile` (alvo `release`)

**Interfaces:**
- Consumes: target `Corvo` da Task 1
- Produces: `make release` gerando `build/Corvo.zip`

- [ ] **Step 1: Adicionar o alvo `release` ao `Makefile`**

```makefile
.PHONY: release

release: gen
	rm -rf build
	xcodebuild -project Corvo.xcodeproj -scheme Corvo -configuration Release \
		-derivedDataPath build/dd build
	mkdir -p build
	ditto -c -k --keepParent build/dd/Build/Products/Release/Corvo.app build/Corvo.zip
	@echo "gerado: build/Corvo.zip"
```

- [ ] **Step 2: Escrever `LICENSE`**

Licença MIT, ano 2026, titular Alexsander. Use o texto padrão da MIT sem
alterações.

- [ ] **Step 3: Escrever `README.md`**

Precisa conter, nesta ordem: o que o app faz em duas frases; captura de tela do
painel; instalação via `brew install --cask corvo`; o aviso de que a build não é
notarizada e a primeira abertura exige Ajustes do Sistema → Privacidade e
Segurança → "Abrir Mesmo Assim"; por que a permissão de Acessibilidade é pedida;
a nota de que **o histórico não é criptografado** e fica em
`~/Library/Application Support/Corvo/`; atalhos (`⌘⇧V`, `←`/`→`, `Enter`, `⌘T`,
`⌘P`, `Delete`, `Esc`); e como compilar (`brew install xcodegen && make build`).

- [ ] **Step 4: Escrever `.github/workflows/release.yml`**

```yaml
name: release

on:
  push:
    tags: ["v*"]

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Instalar XcodeGen
        run: brew install xcodegen
      - name: Compilar
        run: make release
      - name: Publicar release
        uses: softprops/action-gh-release@v2
        with:
          files: build/Corvo.zip
          generate_release_notes: true
```

- [ ] **Step 5: Escrever `Casks/corvo.rb`**

Substitua `SEU_USUARIO` pelo dono do repositório e `SHA_DO_ZIP` pelo resultado de
`shasum -a 256 build/Corvo.zip` após o primeiro release.

```ruby
cask "corvo" do
  version "0.1.0"
  sha256 "SHA_DO_ZIP"

  url "https://github.com/SEU_USUARIO/corvo/releases/download/v#{version}/Corvo.zip"
  name "Corvo"
  desc "Histórico de área de transferência com organização por app e por tags"
  homepage "https://github.com/SEU_USUARIO/corvo"

  depends_on macos: ">= :sonoma"

  app "Corvo.app"

  zap trash: [
    "~/Library/Application Support/Corvo",
    "~/Library/Preferences/com.wylp.corvo.plist",
  ]
end
```

- [ ] **Step 6: Verificar o empacotamento**

Run: `make release`
Esperado: `build/Corvo.zip` existe. Descompacte em `/Applications` e rode o app
de lá; confirme que o atalho `⌘⇧V` funciona fora do Xcode.

- [ ] **Step 7: Commit**

```bash
git add README.md LICENSE .github Casks Makefile
git commit -m "chore: empacotamento, release automatizado e cask do Homebrew"
```

---

## Divergências conscientes do spec

**Arrastar card para a tag na barra lateral não está no plano.** O spec lista
esse gesto junto com `⌘T`. `⌘T` cobre a mesma necessidade — atribuir tag ao item
selecionado — e é o caminho de teclado, que é como o resto do app funciona.
Drag-and-drop no SwiftUI aqui exige `draggable`/`dropDestination` com um tipo
transferível próprio e alvos de drop por linha da lista: código real para um
segundo caminho até o mesmo lugar. Fica de fora da v1; se o gesto fizer falta na
prática, vira uma task própria depois.

Nenhuma outra divergência: todo o resto do spec está coberto abaixo.

## Cobertura do spec

| Requisito do spec | Task |
|---|---|
| App accessory, sem Dock, menu bar | 1 |
| Sem sandbox, hardened runtime, macOS 14+ | 1 |
| Schema SQLite com migrações versionadas | 2 |
| Blobs de imagem em disco | 3 |
| Arquivos por referência, não cópia | 4, 10 |
| Deduplicação por hash preservando tags e pin | 4 |
| Busca casando conteúdo e nome da fonte | 4 |
| Filtros de fonte e tag combinando com AND | 4, 10 |
| Retenção com fixados e tags protegidos | 5 |
| Coleta de blobs órfãos | 3, 5 |
| `org.nspasteboard.source` autoritativa | 7 |
| Heurística de foco com janela de 0,3s | 6 |
| Poll do `changeCount` a 0,3s | 7 |
| Filtro `ConcealedType` e `TransientType` | 7 |
| Blocklist de apps | 7, 12 |
| Atalho global ⌘⇧V | 8 |
| `NSPanel` flutuante, Esc fecha | 8 |
| Carrossel de cards com ícone da fonte | 10 |
| Navegação ←/→, Enter, ⌘T, ⌘P, Delete | 10 |
| Gerenciamento na mesma janela | 10 |
| Arrastar card para tag | **fora da v1** — ver Divergências |
| Colar de volta via CGEvent | 11 |
| Degradação sem Acessibilidade | 11 |
| `SMAppService` login item | 12 |
| Costura com `NSPasteboard` real (TIFF→PNG, arquivos, tipos) | 13 |
| Persistência em disco e migração idempotente | 13 |
| Release GitHub + cask Homebrew | 14 |
| README com aviso de não-notarizado e de histórico não criptografado | 14 |
